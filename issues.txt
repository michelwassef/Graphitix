[fixed 2026-05-05] js/workers/graphArchive.worker.js lagged behind js/shared/graphArchive.js and omitted ui-state.json from worker-built .graph archives, so normal worker-backed saves could drop toolbar/table/component UI state even though the parser and main-thread writer supported it.
[fixed 2026-05-06] .graph archive writing schema was duplicated between js/shared/graphArchive.js and js/workers/graphArchive.worker.js; js/shared/graphArchiveSchema.js now owns README content and per-tab archive file paths, with parity tests covering the worker/main schema.
[fixed 2026-05-05] __tests__/box.statsControls.tabIsolation.test.js was order-dependent when run as a full file: individual failing tests passed alone, but failed after a preceding box-tab test because browser globals persisted across jest.resetModules(); the test now clears window.Main, window.Components, and window.Shared in setup/teardown.
[fixed 2026-05-06] User-origin dirty tracking still partly relied on reason-string classification in Main.session.markSessionDirty; dirty origin is now explicit, lifecycle callers pass origin: 'lifecycle', user paths pass origin: 'user', and missing origin defaults to user-dirty.
[fixed 2026-05-06] js/shared/graphArchive.js wrote ui-state.json but its README contract omitted that file while the worker README listed it; archive writer parity coverage now catches README/manifest/file drift between main-thread and worker builders.
[fixed 2026-05-06] Stats UI restore/presentation tests were order-dependent when run together because full DOM/bootstrap globals could survive across files; the stats UI suites now explicitly clear window/global Main, Components, and Shared during teardown.
[fixed 2026-05-06] js/components/venn.js still wrote Venn analysis/style payload updates through assignTabPayload, bypassing the centralized user-origin dirty contract used by the shared table/style/stat paths; Venn now routes those updates through updateTabPayload when available.
[fixed 2026-05-06] Venn set-label placement assumed SVGTextElement.getBBox was always available, so jsdom or detached SVG contexts could abort drawing before preserveAspectRatio was restored; the renderer now falls back to measured text bounds.
[fixed 2026-05-06] __tests__/venn.upset.test.js contained a leftover DEBUG STAGE PR console.log in a normal assertion path; the noisy debug print was removed.
[fixed 2026-05-06] Shared.hot direct payload mutation assumed every component stores table data as a matrix in payload.data, which could corrupt component-specific object payloads such as Venn lists/labels; table payload updates now allow a component hook while keeping the shared mutation path homogeneous.
[fixed 2026-05-06] Venn user controls still relied on later tab-state capture for several sliders, labels, plot mode, and list/numeric inputs, so recovery/autosave could miss edits until a tab switch or save; these controls now sync the active Venn payload through the centralized user-origin path immediately.
[fixed 2026-05-06] Render-cache restore suppression was only applied after cold component initialization, so first-time reopen of cached tabs could still enqueue layout-sync redraws before the archive cache was restored; componentLayout now stores pending per-tab suppressions and applies them when the layout instance is created.
[fixed 2026-05-06] js/components/line.js ignored the shared skipInitialDraw/restoreRenderCache activation contract and always scheduled an initial draw during setup, unlike components that respect redraw-free archive restores; line now skips its setup draw when domControls is preparing a render-cache restore.
[fixed 2026-05-06] Venn tab activation reused the global notes control when switching to another per-tab root, so notes text/open state could leak across Venn tabs and new empty Venn tabs; initNotes now only reuses a control mounted inside the current tab root.
[fixed 2026-05-06] Venn payload application called syncTableFromInputs without skipPayloadSync, so lifecycle restore could mark and flush a tab before the rest of its style/notes payload had been applied; restore now treats the table sync as lifecycle-only.
[fixed 2026-05-06] Box render-cache restore could suppress the redraw that previously rebuilt statsContext, leaving the Calculate Statistics button disabled on restored tabs without saved stats; box now primes the stats context from the tab-scoped cached draw input after restoring the visual cache.
[fixed 2026-05-07] Shared.hot passed AG Grid option columnBuffer, which AG Grid rejected at runtime; the invalid option plumbing has been removed from js/shared/hot.js (parse/state/update/init paths) to align with current AG Grid option schema.
[fixed 2026-05-07] js/components/venn.js still used global `document`/`#vennPage` fallbacks in root resolution, activation, and preview-source lookup, which could target duplicate-id nodes outside the active mounted tab root. Venn now resolves roots through workspace-tab mounted root first, then static workspace root, without global document fallback.
[fixed 2026-05-07] Toolbar manual tab switching could be immediately overridden by context-dock sync: when a Data transform/font host stayed visible, MutationObserver-driven `syncToolbarContextSection` re-activated Data after clicking General. `js/shared/workspaceToolbar.js` now collapses visible context hosts outside the requested section before manual section activation (click, keyboard, and API), so manual tab intent wins consistently.
[fixed 2026-05-07] Toolbar section switching could still snap back to Data even after host-collapse because context sync always won whenever a dock context remained detectable. `js/shared/workspaceToolbar.js` now suppresses context auto-switch after manual tab selection (`toolbarContextSuppressed`) and only re-enables context takeover when a context-driven host is explicitly shown again.
[fixed 2026-05-07] Clicking toolbar section tabs (e.g., General) after AG Grid cell interaction could still end on Data because capture-phase handlers (transform/context mutations) ran before per-toolbar tab click handling, allowing a revert before manual selection applied. `js/shared/workspaceToolbar.js` now has a document capture-phase section-tab handler that applies manual section selection immediately and atomically (`collapseToolbarContextHosts` + `setToolbarActiveSection`), preventing the Data reversion race.
[fixed 2026-05-07] Duplicate DOM ids across per-tab roots caused brittle control targeting in component flows. Component-level global ID lookups have now been migrated to tab-scoped resolvers (`get*NodeById` / mounted-root lookups) across `scatter.js`, `line.js`, `box.js`, `venn.js`, `pca.js`, `roc.js`, and `survival.js`, eliminating hidden-tab control resolution in multi-tab component interactions. Validation: Playwright `e2e/toolbar.general-vs-data.regression.spec.js` and `e2e/tab-preview-components.spec.js`, plus Jest tab-isolation suites.
[fixed 2026-05-07] Root resolver homogeneity gap: components now resolve roots through a shared `Shared.workspaceTabs.resolveComponentRoot` contract (mounted tab root -> connected current root -> static page root -> null), and resolver helpers no longer fall back to global `document` for cross-tab component root resolution.
[open 2026-05-07] `__tests__/ui.events.test.js` is entirely skipped, leaving toolbar/menu interactions and several cross-component UI pathways unverified in CI. Given repeated regressions around toolbar/context switching, this creates a blind spot; we should unskip or replace with focused active suites for toolbar section switching and context-host behavior.
[fixed 2026-05-07] `js/shared/workspaceToolbar.js` no longer mutates toolbar dataset flags ad hoc in multiple paths: section-state writes are now funneled through `applyToolbarSectionState`, and section activation/context-sync call that central writer to reduce race-prone distributed mutations.
[fixed 2026-05-07] Shared.ensureGraphViewport/Shared.autoResizeSvg no longer default preserveAspectRatio to `none`; defaults now use `xMidYMid meet` so callers that omit explicit preserve mode keep non-distorting SVG behavior by default.
[fixed 2026-05-07] Box tab isolation leak: after resizing Box tab #2 then switching to Box tab #1, resize interactions could be dropped while Box remained in authoritative restore mode. Box now uses bounded restore suppression and explicit release on user resize/layout reasons, preventing stale cross-tab sizing state from persisting across same-component tab switches.
[fixed 2026-05-07] Playwright coverage gap for tab isolation: added `e2e/component.same-type-tab-resize-switch.isolation.spec.js` to exercise panel/svg resize flows and assert requested tab id equals active session id after same-component tab switches.
[fixed 2026-05-07] Box render-cache authoritative restore gating is now bounded (count/time) like line/scatter, rather than unbounded boolean suppression, so restore suppression self-expires and cannot indefinitely block resize/layout redraws.

[open 2026-05-07] Jest module collision risk: local `.claude/worktrees/...` duplicates `package.json` names, triggering `jest-haste-map` naming collisions (`graphitix`, `graphitix-desktop`) when broad test patterns run. We should exclude `.claude/worktrees/**` from Jest roots/module discovery to keep CI deterministic.
[open 2026-05-07] Box significance restore/layout regression: Playwright `e2e/box.significance-restore.spec.js` (and related layout spec) still fails because pairwise significance annotations are not consistently present after compute/restore flows, indicating a remaining bug in Box significance render/restore wiring independent of the tab-resize isolation fix.
[fixed 2026-05-07] Box significance auto-enable timing bug: clicking Calculate statistics with pairwise comparisons enabled by default could still render with significance disabled because showSignificanceBars was only set in post-compute success, after annotation generation already ran. handleStatsComputeClick now promotes auto-enabled significance to state before computeStats, so pairwise lines/labels are generated in the same compute pass.
[fixed 2026-05-07] Resize-only layout changes could be lost on tab deactivation for clean tabs (e.g., scatter tab #2 resize then exit/re-enter): Main.session.persistActiveTabState skip-live-capture path returned before capturing layoutState, so payloadDirty=false lifecycle persists dropped resizer state. The skip path now captures runtime/layout state, updates layout signatures, and refreshes preview when layout changed.
[fixed 2026-05-07] Inactive tab preview updates could clear valid stored previews when 	abHasTableData returned false during hover/lifecycle checks; js/main/previews.js now preserves existing preview markup for hover/lifecycle no-data paths and also retries with force-capture on inactive hover when preview is missing.
[open 2026-05-07] Surface same-type dual-tab preview remains unstable in automated hover sequence (second inactive Surface tab may not show tooltip preview after selection-tab context switch) while other components pass. 2e/component.same-type-dual-preview.spec.js currently excludes surface and this component needs dedicated preview lifecycle investigation.
[fixed 2026-05-07] Box multi-group stats still had one unguarded trace dereference (`traces[i].y`) in `computeStats`, so same-component tab context changes could leave sparse trace entries and crash with `TypeError: traces[i].y is not iterable`. The overall-annotation max-value path now uses guarded `rawY/y` extraction with finite filtering and analysis-group fallback, matching the robust trace access pattern used elsewhere in box stats logic.
[fixed 2026-05-07] Duplicating a Box tab with precomputed stats could leave `state.statsContext.svg` null/stale during activation timing; recompute then reached `annotatePair` with a null target layer and crashed at `appendChild`. Box now resolves/rebinds a live tab-scoped SVG during stats-context priming and again immediately before compute, and defers compute with a clear status when no live SVG is available yet.
