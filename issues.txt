[fixed 2026-05-14] `__tests__/ui.events.test.js` is no longer entirely skipped; the suite now runs (the previous `describe.skip` gate was removed), restoring CI coverage for UI wiring paths.
[fixed 2026-05-14] Jest module collision hardening: `jest.config.js` now ignores `<rootDir>/.claude/worktrees/` in both `modulePathIgnorePatterns` and `testPathIgnorePatterns`, preventing haste-map package-name collisions from local worktrees.
[fixed 2026-05-14] Surface same-type dual-tab preview coverage is restored: `e2e/component.same-type-dual-preview.spec.js` now includes `surface` in `COMPONENTS_WITH_DUAL_PREVIEW_COVERAGE`.
[fixed 2026-05-14] Duplicate-tab flow was unified around a shared execution path: `js/main/tabs/duplicatePrompt.js` now exposes `applyDuplicateChoice(...)`, and `js/main/tabs.js` context-menu duplication reuses this helper instead of maintaining a second divergent clone/empty implementation.
[fixed 2026-05-14] Preview eligibility now uses a component-aware readiness path in `js/main/previews.js` (`hasComponentAwarePreviewReadiness`) with support for component hooks (`config.isPreviewReady`) and render-state signals, reducing reliance on generic `session.tabHasTableData` alone.
[fixed 2026-05-14] Dual-preview flakiness mitigation: `e2e/component.same-type-dual-preview.spec.js` now waits for persisted preview metadata (`previewMeta.updatedAt`) after forced capture and uses a longer bounded retry loop to reduce async timing races under CDN-override mode.
[fixed 2026-05-14] Dark-theme ownership was normalized through shared theme resolution: `js/shared/colorSchemes.js` now exports `resolveThemeState(type, payload)`, and component theme branches in `venn.js`, `surface.js`, `pca.js`, `line.js`, `scatter.js`, and `box.js` now consume the shared resolver instead of ad hoc dark checks.
[fixed 2026-05-14] Render-commit barrier introduced in session state: tabs now carry `payloadVersion`, `layoutVersion`, and `renderCommitVersion`; archive render-cache peek/consume paths enforce commit freshness, and preview capture marks committed render state (`markTabRenderCommitted`) to prevent stale visual snapshots from being treated as authoritative.
[fixed 2026-05-14] Global dirty tracking is now declarative-capable: `js/main/session.js` user-input listener honors `data-session-ignore-dirty="1"` and `data-session-affects-payload="0"`, and toolbar autosave control now advertises these attributes (`js/shared/workspaceToolbar.js`) to avoid non-payload UI interactions being misclassified as payload edits.
[open 2026-05-14] Cross-component activation/theming reconciliation remains partially duplicated: theme-sensitive visual refresh relies on mixed mechanisms (polling in colorSchemes, component-local activation restore hooks, and lifecycle draw events). This should be unified behind a single shared post-activation reconcile contract (with deterministic ordering after render-cache restore + first draw) to prevent repeated stale-control regressions across components.
[open 2026-05-14] Render-cache architecture is conflating static markup snapshots with live interactive runtime: `session.cloneRenderCacheForStorage` serializes DOM (`serializeDomNode`/`deserializeRenderCacheValue`) which strips JS event listeners, then activation paths can restore this cache (`domControls` render-cache restore) while lifecycle suppression skips redraw/rebind. Result: restored graphs can look correct but lose interactive controls (e.g., legend drag) and keep stale visual/control state.
[open 2026-05-14] Tab isolation relies on mutable component singletons (`pcaState`, `scatterState`, etc.) plus opportunistic capture/apply hooks instead of strictly tab-scoped state containers with deterministic activation reconciliation. Under fast-path activation, render-cache restore, and draw suppression, singleton runtime state and restored DOM can diverge, causing cross-tab inconsistencies (theme/control/state drift).
