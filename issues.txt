[open 2026-05-14] Cross-component activation/theming reconciliation remains partially duplicated: theme-sensitive visual refresh relies on mixed mechanisms (polling in colorSchemes, component-local activation restore hooks, and lifecycle draw events). This should be unified behind a single shared post-activation reconcile contract (with deterministic ordering after render-cache restore + first draw) to prevent repeated stale-control regressions across components.
[open 2026-05-14] Render-cache architecture is conflating static markup snapshots with live interactive runtime: `session.cloneRenderCacheForStorage` serializes DOM (`serializeDomNode`/`deserializeRenderCacheValue`) which strips JS event listeners, then activation paths can restore this cache (`domControls` render-cache restore) while lifecycle suppression skips redraw/rebind. Result: restored graphs can look correct but lose interactive controls (e.g., legend drag) and keep stale visual/control state.
[open 2026-05-14] Tab isolation relies on mutable component singletons (`pcaState`, `scatterState`, etc.) plus opportunistic capture/apply hooks instead of strictly tab-scoped state containers with deterministic activation reconciliation. Under fast-path activation, render-cache restore, and draw suppression, singleton runtime state and restored DOM can diverge, causing cross-tab inconsistencies (theme/control/state drift).
[open 2026-05-15] Aspect-lock default policy is duplicated across resizer/componentLayout/graphSizing/chartStyle with separate boolean coercion paths. Even after aligning defaults to unlocked, this remains an architectural risk: introduce a single shared resolver for initial aspect-lock state (new tab, restored tab, imported file) and make all modules consume it to prevent future semantic drift.
[open 2026-05-15] Activation lifecycle contract remains partially component-local: `bindTabActivation` exits early on DOM rebind and does not enforce a shared post-activation render-readiness check. Components are forced to hand-roll recovery (hot rebind + graph-ready probe + fallback draw), which should be centralized in `Shared.componentLifecycle` to prevent repeated tab-switch regressions.
[open 2026-05-15] Architectural consistency risk: component-level table access patterns are still heterogeneous (some paths directly use cached state.hot/pcaHotInstance while others enforce ensureTableForTab/ensureHotForActiveTab). Standardize a shared tab-scoped table accessor contract across all components to prevent future cross-tab payload/caching drift regressions.
[open 2026-05-15] Clean-tab payload projection remains non-deterministic across components (regular persistActiveTabState payload drift observed on untouched tabs). Even with recovery-interval cache-recapture mitigation in session, this is an architectural inconsistency: payload builders should be normalized to deterministic, idempotent output so lifecycle checkpoints do not continuously mutate signatures.
[open 2026-05-15] Runtime mutator tab-scoping is still duplicated across modules (`colorSchemes`, `publicationStyles`, and component-local helpers each hand-roll mounted-root resolution). Introduce one shared tab-scoped DOM mutator utility (root resolution + active-session guard + optional deferred retry) and migrate mutator paths to it to prevent future selector-scope regressions.
[open 2026-05-15] Multiple components still hardcode `chartStyle.TEXT_COLOR` in render paths while also supporting per-tab theme payloads; this dual model relies on post-render theme mutation for correctness and increases leak risk under cache/activation fast paths. Normalize components onto payload-resolved per-tab text/axis color usage and treat post-render themeing as fallback only.
[open 2026-05-15] Theme payload schema still redundantly stores both `colorScheme` and resolved tokens (`textColor/backgroundColor`) for several components. This duplication allows signature drift and stale mixed-theme tuples under lifecycle checkpoints. Architectural fix: persist only canonical scheme ID (plus true user-editable overrides when such UI exists), and derive non-editable tokens at render time.
[open 2026-05-16] Control-ID architecture still assumes globally unique IDs for many per-component inputs (font, axis, style controls), forcing occasional document-level fallback lookups in shared helpers. As same-type tabs keep independent mounted roots, this remains a latent tab-isolation risk; migrate control discovery to tab-root-scoped selectors/data attributes and remove global ID fallback paths entirely.
[open 2026-05-16] Component runtime snapshot schemas remain inconsistent across modules (some include theme state, some omit it). Define a shared runtime-snapshot contract in Shared.componentLifecycle (required fields + migration hooks) so cross-tab activation cannot regress through omitted per-tab visual state.
[open 2026-05-16] Scatter initial warmed switching currently reports cache-rejected-no-redraw-observed (runtime-cache=0/2) while most peer components still report runtime cache reuse in the same phase. This is a cross-component cache-path inconsistency worth normalizing so isolation safety and warm-switch performance stay aligned.
[open 2026-05-16] Resizer-control relocation (moving config-panel controls into `.resizer-options-menu`) is still implemented ad hoc in each component with component-local cached DOM refs. This pattern caused second-tab relocation drift in PCA/Pie and is a broader architectural risk; extract a shared tab-scoped control-relocation helper with active-root rebinding and lifecycle hook integration.
[open 2026-05-16] Venn initial warmed switching currently reports cache-rejected-no-redraw-observed (runtime-cache=0/2) while most peer components report runtime cache reuse in the same phase. Normalize Venn warm-switch cache path with peer components to reduce behavioral divergence and future isolation regressions.
