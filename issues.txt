[fixed 2026-05-14] `__tests__/ui.events.test.js` is no longer entirely skipped; the suite now runs (the previous `describe.skip` gate was removed), restoring CI coverage for UI wiring paths.
[fixed 2026-05-14] Jest module collision hardening: `jest.config.js` now ignores `<rootDir>/.claude/worktrees/` in both `modulePathIgnorePatterns` and `testPathIgnorePatterns`, preventing haste-map package-name collisions from local worktrees.
[fixed 2026-05-14] Surface same-type dual-tab preview coverage is restored: `e2e/component.same-type-dual-preview.spec.js` now includes `surface` in `COMPONENTS_WITH_DUAL_PREVIEW_COVERAGE`.
[fixed 2026-05-14] Duplicate-tab flow was unified around a shared execution path: `js/main/tabs/duplicatePrompt.js` now exposes `applyDuplicateChoice(...)`, and `js/main/tabs.js` context-menu duplication reuses this helper instead of maintaining a second divergent clone/empty implementation.
[fixed 2026-05-14] Preview eligibility now uses a component-aware readiness path in `js/main/previews.js` (`hasComponentAwarePreviewReadiness`) with support for component hooks (`config.isPreviewReady`) and render-state signals, reducing reliance on generic `session.tabHasTableData` alone.
[fixed 2026-05-14] Dual-preview flakiness mitigation: `e2e/component.same-type-dual-preview.spec.js` now waits for persisted preview metadata (`previewMeta.updatedAt`) after forced capture and uses a longer bounded retry loop to reduce async timing races under CDN-override mode.
[fixed 2026-05-14] Dark-theme ownership was normalized through shared theme resolution: `js/shared/colorSchemes.js` now exports `resolveThemeState(type, payload)`, and component theme branches in `venn.js`, `surface.js`, `pca.js`, `line.js`, `scatter.js`, and `box.js` now consume the shared resolver instead of ad hoc dark checks.
[fixed 2026-05-14] Render-commit barrier introduced in session state: tabs now carry `payloadVersion`, `layoutVersion`, and `renderCommitVersion`; archive render-cache peek/consume paths enforce commit freshness, and preview capture marks committed render state (`markTabRenderCommitted`) to prevent stale visual snapshots from being treated as authoritative.
[fixed 2026-05-14] Global dirty tracking is now declarative-capable: `js/main/session.js` user-input listener honors `data-session-ignore-dirty="1"` and `data-session-affects-payload="0"`, and toolbar autosave control now advertises these attributes (`js/shared/workspaceToolbar.js`) to avoid non-payload UI interactions being misclassified as payload edits.
[open 2026-05-14] Cross-component activation/theming reconciliation remains partially duplicated: theme-sensitive visual refresh relies on mixed mechanisms (polling in colorSchemes, component-local activation restore hooks, and lifecycle draw events). This should be unified behind a single shared post-activation reconcile contract (with deterministic ordering after render-cache restore + first draw) to prevent repeated stale-control regressions across components.
[open 2026-05-14] Render-cache architecture is conflating static markup snapshots with live interactive runtime: `session.cloneRenderCacheForStorage` serializes DOM (`serializeDomNode`/`deserializeRenderCacheValue`) which strips JS event listeners, then activation paths can restore this cache (`domControls` render-cache restore) while lifecycle suppression skips redraw/rebind. Result: restored graphs can look correct but lose interactive controls (e.g., legend drag) and keep stale visual/control state.
[open 2026-05-14] Tab isolation relies on mutable component singletons (`pcaState`, `scatterState`, etc.) plus opportunistic capture/apply hooks instead of strictly tab-scoped state containers with deterministic activation reconciliation. Under fast-path activation, render-cache restore, and draw suppression, singleton runtime state and restored DOM can diverge, causing cross-tab inconsistencies (theme/control/state drift).
[open 2026-05-14] Live-DOM fast-path activation currently assumes previously mounted markup remains renderable; when a tab is reactivated with hidden/empty graph DOM, the fast path can return success while lifecycle suppression blocks immediate redraw, leaving blank outputs. Introduce a shared fast-path readiness contract that validates renderable graph content (or required draw fallback) before accepting passive activation for any component.
[fixed 2026-05-14] Tab-isolation regression harness now fails when a switched tab has no renderable graph: __tests__/tab-isolation-regression/renderer-harness.js previously computed hasGraph but did not convert hasGraph === false into a test failure, allowing blank-graph regressions to pass silently.
[fixed 2026-05-14] Live-DOM reuse validation is now visibility-aware across runtime activation: js/shared/componentLifecycle.js hasRenderableGraphContent was upgraded from markup-only checks to visible-render checks, and js/main/domControls.js now defers to this shared validator before accepting per-tab fast-path reuse.
[fixed 2026-05-14] Tab-isolation regression now fails on invisible/blank switched graphs, not just missing SVG nodes: __tests__/tab-isolation-regression/renderer-harness.js graph probe was upgraded to validate visibility chain, container geometry, and drawable SVG/canvas primitives before counting a tab as successfully rendered.
[open 2026-05-14] HOT tab-mount API usage is inconsistent across components (Shared.hot.mountTableForTab vs Shared.hot.ensureTableForTab). The low-level mount path bypasses mounted-root reconciliation and tab-id inference safeguards, increasing stale-root risk after same-component tab duplication; remaining components should be normalized to nsureTableForTab.
[open 2026-05-15] Aspect-lock default policy is duplicated across resizer/componentLayout/graphSizing/chartStyle with separate boolean coercion paths. Even after aligning defaults to unlocked, this remains an architectural risk: introduce a single shared resolver for initial aspect-lock state (new tab, restored tab, imported file) and make all modules consume it to prevent future semantic drift.
[fixed 2026-05-15] Tab-isolation regression now validates each component’s primary graph host (e.g., `#pcaPlot`, `#scatterPlot`) instead of only workspace-root graph primitives, so auxiliary plots (like PCA scree) can no longer mask a missing main chart.
[open 2026-05-15] Activation lifecycle contract remains partially component-local: `bindTabActivation` exits early on DOM rebind and does not enforce a shared post-activation render-readiness check. Components are forced to hand-roll recovery (hot rebind + graph-ready probe + fallback draw), which should be centralized in `Shared.componentLifecycle` to prevent repeated tab-switch regressions.
[open 2026-05-15] Snapshot intent is encoded via free-form reason strings (`dirty`, `recovery-interval`, `autosave`, `archive-save`) in `sessionActions.persistActiveTabIfNeeded`, which lets background recovery checkpoints accidentally recapture render cache during live edits. Replace reason-text heuristics with explicit snapshot mode flags (checkpoint vs export, cache-capture policy, preview-capture policy) to make cache behavior deterministic.
[fixed 2026-05-15] Snapshot-intent handling was normalized away from reason-string heuristics: `sessionActions` now resolves explicit `snapshotKind`/`snapshotIntent` profiles, lifecycle paths pass checkpoint intent directly, and `session.persistActiveTabState` honors intent flags for deterministic live-payload capture/drift-probe/cache behavior.
[fixed 2026-05-15] Line homogeneous-switch regression was root-caused to non-idempotent 2D/3D mode transitions; repeated same-mode lifecycle activations could re-transform table state until no Y series remained. `line.enterLine3dMode`/`exitLine3dMode` are now idempotent no-op-safe refresh paths, preserving renderability across rapid tab switches and reopen flows.
[open 2026-05-15] Snapshot-intent profile objects are currently duplicated in multiple modules (`tabs.js`, `tabs/unsavedPrompt.js`, `documentState.js`, `sessionActions.js`). Move these profiles behind a single shared factory/enum to prevent semantic drift between lifecycle checkpoint callers.
[fixed 2026-05-15] Tab-isolation regression runner pathing drift was corrected: __tests__/tab-isolation-regression/run-regression.ps1 and its README examples now consistently reference __tests__/tab-isolation-regression (the script previously invoked tests/tab-isolation-regression/run-regression.js, causing MODULE_NOT_FOUND).
[open 2026-05-15] Tab-isolation harness path conventions are still split between __tests__ and tests: while run-regression.ps1 now launches __tests__/tab-isolation-regression/run-regression.js, the JS runner default artifact directory still resolves under tests/tab-isolation-regression/artifacts. Centralize harness root-path resolution in one shared constant to prevent future path drift across script/docs/runner outputs.
[fixed 2026-05-15] Tab-isolation harness root-path consistency was centralized in __tests__/tab-isolation-regression/run-regression.js via shared constants for harness path and artifacts path; usage/help and default outDir now derive from one source (__tests__/tab-isolation-regression), eliminating tests-vs-__tests__ drift.
[fixed 2026-05-15] Pie tab-isolation regression failures were root-caused to stale HOT instance usage in tab-sensitive paths (example load, import, and getPayload) that preferred state.hot over tab-scoped ensureHotForActiveTab; pie now resolves the active tab HOT first and self-heals graph presence before render-cache capture.
[open 2026-05-15] Architectural consistency risk: component-level table access patterns are still heterogeneous (some paths directly use cached state.hot/pcaHotInstance while others enforce ensureTableForTab/ensureHotForActiveTab). Standardize a shared tab-scoped table accessor contract across all components to prevent future cross-tab payload/caching drift regressions.
[open 2026-05-15] Clean-tab payload projection remains non-deterministic across components (regular persistActiveTabState payload drift observed on untouched tabs). Even with recovery-interval cache-recapture mitigation in session, this is an architectural inconsistency: payload builders should be normalized to deterministic, idempotent output so lifecycle checkpoints do not continuously mutate signatures.
[fixed 2026-05-15] Color-scheme application no longer relies on skippable clean-tab snapshots: `js/shared/colorSchemes.js` now forces live payload capture on `color-scheme-pre-*` persists (`forceLivePayloadCapture` + non-skippable snapshot intent), preventing stale header-only payload projection from replacing loaded tab data during theme updates.
[open 2026-05-15] Scatter dark-theme cross-tab visual leak: js/shared/colorSchemes.js applies rendered theme at page scope (pplyRenderedTheme iterates all .svgbox under #scatterPage) even when called from pplyRenderedThemeForTab(...). In per-tab DOM mode this mutates inactive tab DOM visuals (axis/tick colors) and violates tab isolation. Theme application should be scoped to the active tab root/session record, not component page-wide.
[fixed 2026-05-15] Runtime theme/style mutators were normalized to tab-scoped DOM roots: `js/shared/colorSchemes.js` and `js/shared/publicationStyles.js` now resolve mounted roots via `Shared.workspaceTabs` before mutating `.svgbox`/SVG visuals, eliminating same-component cross-tab visual bleed from page-scoped selectors.
[fixed 2026-05-15] Tab-isolation regression coverage now includes an explicit same-type theme leakage probe: `__tests__/tab-isolation-regression/renderer-harness.js` added `runThemeIsolationPhase` (scatter scientific-vs-dark sibling check), wired by `run-regression.js` as `08_theme_isolation`, and documented in the harness README.
[open 2026-05-15] Runtime mutator tab-scoping is still duplicated across modules (`colorSchemes`, `publicationStyles`, and component-local helpers each hand-roll mounted-root resolution). Introduce one shared tab-scoped DOM mutator utility (root resolution + active-session guard + optional deferred retry) and migrate mutator paths to it to prevent future selector-scope regressions.
[fixed 2026-05-15] Scatter runtime text/axis/title drawing now uses the tab-resolved theme color (`scatterThemeTextColor`) instead of hardcoded `chartStyle.TEXT_COLOR` in 2D axis/tick/title/annotation paths, removing a residual cross-tab color coupling channel when themes differ between sibling scatter tabs.
[open 2026-05-15] Multiple components still hardcode `chartStyle.TEXT_COLOR` in render paths while also supporting per-tab theme payloads; this dual model relies on post-render theme mutation for correctness and increases leak risk under cache/activation fast paths. Normalize components onto payload-resolved per-tab text/axis color usage and treat post-render themeing as fallback only.
[fixed 2026-05-15] Scatter theme payload normalization now treats `textColor/backgroundColor` as scheme-derived state (not authoritative user overrides): `js/components/scatter.js` now resolves these values deterministically from `colorScheme` during apply/draw/getPayload, which heals stale mismatched theme triples (`scheme`, `textColor`, `backgroundColor`) and restores consistent dark-background + axis/tick rendering across tabs.
[open 2026-05-15] Theme payload schema still redundantly stores both `colorScheme` and resolved tokens (`textColor/backgroundColor`) for several components. This duplication allows signature drift and stale mixed-theme tuples under lifecycle checkpoints. Architectural fix: persist only canonical scheme ID (plus true user-editable overrides when such UI exists), and derive non-editable tokens at render time.
[open 2026-05-16] Control-ID architecture still assumes globally unique IDs for many per-component inputs (font, axis, style controls), forcing occasional document-level fallback lookups in shared helpers. As same-type tabs keep independent mounted roots, this remains a latent tab-isolation risk; migrate control discovery to tab-root-scoped selectors/data attributes and remove global ID fallback paths entirely.
[fixed 2026-05-16] Scatter tab-theme leakage root cause fixed by normalizing theme state into per-tab runtime snapshots and payload-first resolution: js/components/scatter.js now captures/applies runtime.theme on deactivate/activate, avoids singleton-only fallback precedence, and applies 2D/3D dark background directly from the active tab's resolved theme snapshot.
[open 2026-05-16] Component runtime snapshot schemas remain inconsistent across modules (some include theme state, some omit it). Define a shared runtime-snapshot contract in Shared.componentLifecycle (required fields + migration hooks) so cross-tab activation cannot regress through omitted per-tab visual state.
[open 2026-05-16] Shared categorical palette defaults are still backed by mutable global arrays (Shared.palette.DEFAULT_SCATTER_COLORS/window.DEFAULT_SCATTER_COLORS) while multiple components derive runtime fallback series/label colors from those arrays. Active-tab theme sync mutates the shared palette, so untouched same-type tabs can rehydrate with the wrong fallback colors (symbol/series leak) and trigger payload drift when label maps are sparse. Architectural fix: replace global mutable palette defaults with tab-scoped scheme palette resolvers in each component (or one shared resolver API) and reserve global palette only for static app defaults.
