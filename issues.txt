# Open Issues (Reviewed 2026-06-01)
# Ordered by descending priority. Closed and stale items were removed.

[open][P0][2026-06-01] Restore-draw scheduling remains fragmented and brittle across components: resize cache invalidation policy, reason-string suppression, and draw-option merge precedence are not centralized, so user-initiated redraws can still be suppressed or over-expanded after reopen. Replace with one shared scheduler/suppression + merge policy contract. (merged from 2026-05-31 issues 89, 90, 91, 95)

[open][P0][2026-06-01] Stats restore authority is still inconsistent between payload apply and activation/draw suppression flows; restored stats metadata can diverge from runtime signature inputs and be invalidated later. Standardize one payload-time stats hydration contract across components before context priming. (merged from 2026-05-22 issues 43, 44)

[open][P0][2026-06-01] Scatter/box payload serialization and control state acquisition still depend on live DOM in critical paths instead of state-first snapshots, keeping inactive-tab/headless flows fragile. Introduce a shared state-authoritative accessor contract. (from 2026-05-19 issue 32)

[open][P1][2026-06-01] Render-cache behavior lacks a shared computation/cache metadata contract and still duplicates canvas bitmap hydration logic. Heavy tabs can reopen visually but miss warm computational caches, then stall on first interaction. Consolidate capture/serialize/restore/validate for computation and bitmap caches. (merged from 2026-05-23 issue 45 and 2026-05-31 issue 92)

[open][P1][2026-06-01] Viewport-fit behavior remains inconsistent across components (2D/3D sequencing, lock semantics, preserve/fill modes), producing cross-component resize/render drift and avoidable letterboxing divergences. Define one shared viewport policy with explicit modes and phase timing. (merged from 2026-05-18 issues 25, 27, 29 and 2026-05-30 issue 85) [2026-06-02 update] box.js drawBox still carries its own viewport routine (preserveBaseAspect viewBox padding + default 'meet') that duplicates the aspect-preservation now added to shared autoResizeSvg (baseViewport option). line/scatter/pca 2D now route through the shared helper; box should be migrated onto it too and the bespoke box routine removed.

[open][P1][2026-06-01] Bottom-axis reserve/rotation behavior is still partly duplicated and contract-incomplete (`computeBottomLayout` always bakes axis-title reserve). Add explicit shared axis-title/reserve options and component-wide rotation hysteresis adoption. (merged from 2026-05-18 issues 26, 30)

[open][P1][2026-06-01] Shared chart-style/theming helpers remain fragmented: resize scale policy is global-only, `applySvgDefaults` still mixes style with interactive font-control binding, and line/scatter SVG theme logic is duplicated across draw paths. Split responsibilities and centralize theme/style application helpers. (merged from 2026-05-18 issues 22, 23, 24)

[open][P1][2026-06-01] Box color intent is still inferred from color value (`isBoxThemeNeutralColorToken`) rather than explicit user-intent flags, so value collisions with defaults can still remap deliberate choices. Persist explicit per-color intent/state. (from 2026-05-30 issue 86)

[open][P1][2026-06-01] SVG frame publication lifecycle is still not standardized (build->validate token->publish->commit). Some paths can expose partial/stale intermediate frames. Introduce a shared frame publication contract with cancellation cleanup. (from 2026-05-26 issue 71)

[open][P1][2026-06-01] `workspaceTabs.ensureActiveDomBindings` can still be misused; histogram remains sentinel-driven without explicit current-root reporting, allowing unnecessary rebind/init churn risk. Harden API expectations and add cross-component activation binding tests. (from 2026-05-26 issue 72)

[open][P1][2026-06-02] Runtime snapshot tests do not yet enforce checkbox/control parity across all components. A line same-component activation regression showed live checkbox state can diverge from component-owned runtime shadow fields, then be restored as stale state. Add shared cross-component contracts that toggle critical controls, capture runtime/payload state, switch same-type tabs, and assert DOM/runtime/payload parity. [2026-06-02 RESOLVED for heatmap] TEST-BLINDNESS: jsdom suites (round-trip + integration) cannot observe this class because getBoundingClientRect returns 0 / no layout runs, so rendered text geometry is unmeasurable there -- a jsdom "geometry" test passes while the live layout bug persists (verified). The only real-layout coverage, e2e/heatmap.correlation-tab-restore.spec.js, measures true geometry but exercises ONLY tab-switch restore; the reported bug is the AUTOSAVE-RECOVERY / file-reopen path while the heatmap tab is in the BACKGROUND (user switched to the Welcome tab, reloaded, accepted recovery). ROOT CAUSE: the archive/recovery render cache rehydrates the SVG markup but NOT the component's private render state (lastRenderModel + lastViewOptions + textAspectMetrics all null after restore -- proven). The heatmap's readable-label and cell-value text scales (resolveHeatmapReadableTextScale / resolveHeatmapCellValueTextScale) are derived from textAspectMetrics; with them null, applyTextAspectCorrection (run by the post-activation resize refresh at the correct settled size) falls back to defaults -> labels lose their up-scale (~2.37x->1.0x, tiny) and cell values get full up-scale without the cell-fit reduction (~1.57x->2.41x, overlapping). Only TEXT is affected (viewBox/svgBox/cells identical). FIX: js/components/heatmap.js scheduleHeatmapResizeRefresh.runRefresh -- when state.textAspectMetrics is missing (restored-without-model) and the tab is visible with a hot table + svg, drive draw() DIRECTLY (bypassing the suppressed post-restore scheduler -- see P0 restore-draw suppression issue above) to recompute model+metrics+correction at the settled size, instead of applying a metrics-less correction. New guard test: e2e/heatmap.reopen-recovery.geometry.spec.js (file-reopen, crash-recovery, and the background-recovery repro). FOLLOW-UPS: (a) the archive/recovery render cache dropping component private render state is the general defect (see render-cache computation-metadata issue above) -- other components that derive render details from private state not carried in the archive cache may have analogous reopen corruption; audit + generalize to a cross-component reopen geometry-parity contract. (b) restore-draw suppression swallowed a normal scheduleDraw recompute here, forcing the direct-draw workaround -- fold into the shared restore-draw scheduler/suppression contract (P0 above).

[open][P1][2026-06-01] Scatter still carries architecture drift in hot access and stats draw suppression: `ensureScatterHotForActiveTab` performs repeated header/nested-header side effects, and non-visual stats suppression scaffolding remains complex/defensive. Move side effects to true change events and simplify stats draw flow. (merged from 2026-05-30 issues 78, 79)


[open][P2][2026-06-01] Scatter E2E regression tests still include stale/flaky patterns: an inverted reproduction assertion (`scatter.tab-grid-leak.repro`) and timeout-driven async races (`scatter.csv-import.mixed-tabs.reopen`, `scatter.manual-view-refresh`). Convert to deterministic positive contracts/signals. (merged from 2026-05-30 issues 80, 83, 88)

[open][P2][2026-06-01] Test architecture is still fragmented: unit/integration suites do not consistently use one tab-scoped bootstrap harness, and AG Grid column-order mocks remain duplicated/drift-prone. Standardize bootstrap + reusable column-order mock factories. (merged from 2026-05-19 issues 33, 34, 37)

[open][P2][2026-06-01] Core runtime modules remain monolithic (e.g., box/scatter/hot/line/pca), keeping defect isolation and safe refactoring difficult. Split by domain boundaries with dedicated tests per slice. (from 2026-05-24 issue 47)

[open][P2][2026-06-01] Component-contract documentation generation still hardcodes payload baselines and can drift from live `getPayload` behavior (notably heatmap/surface stats persistence). Add source-derived validation and CI drift checks. (from 2026-05-24 issue 49)

[open][P2][2026-06-01] Quality gates remain incomplete for professional hardening: no lint/type/static analysis lane and low global coverage thresholds. Add explicit non-test quality lanes and enforce in CI. (from 2026-05-24 issue 50)

[open][P2][2026-06-01] Lifecycle contract installation remains fail-soft via no-op capture/apply defaults, allowing components to appear lifecycle-capable without meaningful runtime persistence. Separate capability declaration from fallback behavior and enforce with contract tests. (from 2026-05-24 issue 54)

[open][P3][2026-06-01] Runtime dependency delivery is still split between CDN HTML includes and locally patched test dependencies, which weakens deploy/runtime parity. Move to consistent vendored/bundled dependency strategy. (from 2026-05-24 issue 53)

[open][P3][2026-06-01] Unused scaffold modules (`js/app.js`, `js/ui.js`, `js/utils.js`) are still outside bootstrap and effectively dead; remove them or integrate them into the real runtime path. (from 2026-05-24 issue 48)

[open][P1][2026-06-02] PCA runtime snapshot omitted resolved per-tab colors (pcaLabelColors/pcaFill/border/styles), capturing only theme.colorScheme. On a same-component tab switch the activation path restores the runtime snapshot instead of re-applying the payload, so the redraw used the previously rendered tab's palette: scheme id showed grayscale while the graph kept the sibling's default colors (picker flashed to Custom). FIXED in pca.js captureRuntimeState/applyRuntimeState by carrying a `colors` slice (normalizes PCA to scatter, whose runtime snapshot already carries theme.fill/labelColors). GENERALIZE: audit line/box/hist/pie/roc/survival/heatmap/surface runtime snapshots for the same resolved-color/visual-state omission, and add a cross-component contract test that renders a non-default scheme on one tab, switches to a default sibling and back, and asserts the rendered colors (not just the scheme id) survive. Relates to the 2026-06-02 control-parity entry above.

[open][P1][2026-06-03] Bulk hot.loadData() does not sync the owner tab's payload, so a CSV/import on a single never-deactivated tab leaves tab.payload as the empty-default template while the live component (getPayload) holds the real data. ROOT CAUSE: Shared.hot only syncs tab.payload for per-cell afterChange (syncActiveTabPayloadDataChanges); afterLoadData just fires a draw and is deliberately classified non-user (so reopen stays invisible), so it neither syncs the payload nor marks the tab payloadDirty. Components then diverge: scatter ends up payloadDirty+null-payload, box happens to sync, but line/hist/heatmap end up clean+stale-empty. Any consumer that reads tab.payload via a lifecycle "skip if clean" flush therefore sees no data: writeRecoverySnapshot skipped/cleared the snapshot (no recovery until a tab switch flushed via getPayload), AND shouldWarnBeforeUnload suppressed the unsaved-changes warning. FIXED by forcing a live payload capture (captureLivePayload/allowSkipLivePayloadCapture:false) in documentState.flushActiveTabForRecovery and in sessionActions.shouldWarnBeforeUnload, so both read authoritative live state like a save does. Verified across all 5 table components in e2e/recovery.single-tab.live-capture.spec.js; unit guard in documentState.recoveryThrottle.test.js. Two follow-on defects this surfaced, both fixed: (a) FEEDBACK LOOP — forcing the full (non-skip) persist made persistActiveTabState.markSessionDirty fire on every recovery write (live-DOM layout drift => layoutChanged=true), bumping sessionRevision, which rescheduled another recovery write, whose capture re-detected drift... an unbounded heavy loop (endless debug logs, 500ms setTimeout violations). Fixed with a snapshotCapture intent flag that suppresses the markSessionDirty bump for recovery/beforeunload captures so they are revision-neutral (session.js); guard e2e/recovery.no-loop.spec.js. (b) WRITE LATENCY — the snapshot was written on a per-change debounce (RECOVERY_DELAY_MS, inflated to 5-8s for large payloads) that restarts on every change, so the post-import settle burst kept sliding it for several seconds; reloading in the first 1-2s lost recovery. Fixed with RECOVERY_MAX_WAIT_MS bounding the wait from the first pending change (~1s) regardless of churn; guard e2e/recovery.single-tab.e2e.spec.js. GENERALIZE (still open): the real homogeneity gap is that a user-initiated bulk load should sync tab.payload / mark dirty just like a cell edit — thread a user-vs-system source through loadData so afterLoadData can sync on user import without breaking reopen invisibility, then the "force live capture" guards become belt-and-suspenders. Audit remaining graphTabsHaveData/tabHasTableData callers for the same stale-clean-payload blind spot. Related to the 2026-05-19 "state-first snapshot accessor" P0 above.

[open][P2][2026-06-02] colorSchemes apply grows config.labelColors on every application: each color-scheme change re-adds PCA loadings/variable names (e.g. Variable, Var1..Var4) into the sample labelColors map alongside the real group keys, so the payload signature climbs across repeated applies (observed 18856 -> 20678 -> 21283 -> 23105). Sample-group label colors and loadings-variable colors should not be conflated; scope labelColors to actual sample group keys so repeated scheme changes are idempotent.

[open][P1][2026-06-03 RESOLVED for pca] PCA stats sub-plots were lost on reopen/recovery: reopening a saved .graph PCA tab dropped the scree plot, biplot AND summary; crash recovery dropped the same. Confirmed instance of the general defect predicted in the 2026-06-02 heatmap reopen-recovery FOLLOW-UP (a) "archive/recovery render cache rehydrates SVG markup but not component private render state; other components may have analogous reopen corruption." ROOT CAUSE (pinned with an instrumented chromium run): the PCA render cache snapshotted the stats-panel DOM (captureRenderCache detached #pcaStatsResults + #pcaStatsSummary + #pcaScreePlot, restoreRenderCache replayed them). On the archive path the snapshot is serialized to markup (session.js serializeFragmentPayload) and deserialized into NEW nodes, so restoreChildren(#pcaStatsResults, cache.stats) replaced the live stats subtree while the component's MODULE-LEVEL closure node refs (pcaScreePlot/pcaStatsSummary/pcaScreeContainer/pcaScreeVarianceRow) still pointed at the now-orphaned old nodes. Every subsequent stats render — including loadFromPayload→restorePcaStatsFromPayload, the deferred finalize, and even a forced draw() — wrote scree/biplot/summary into orphaned nodes, so nothing appeared in the document (the eigen table survived only because its rows rode along as static markup inside cache.stats; lastPcaStats itself was always restored intact, proven via getPayload after restore). The data was fine; only the DOM replay was broken. FIX (js/components/pca.js, homogeneity per Rule 8, no new layer): the render cache now carries ONLY the expensive graph (#pcaPlot) + the computational runtimeCache; the stats panel is rebuilt from lastPcaStats (the single source of truth) on every restore via the existing restorePcaStatsFromPayload, so node refs are never orphaned and the interactive scree/biplot controls are re-bound. Deleted: the summary/scree sub-fragment capture+restore, the uiState element-state snapshot, the scree/summary-only fallback, and the now-dead captureElementState/restoreElementState helpers. lastPcaStats is set for the target tab by loadFromPayload/applyRuntimeState before restoreRenderCache runs (componentLifecycle restore order), so tab isolation (Rule 6) holds. Guards added: e2e/pca.stats-restore.spec.js (real-browser file-reopen + crash-recovery asserting #pcaScreePlot svg, visible #pcaBiplotCard with vector lines, eigen rows, summary text); __tests__/stats.ui.persistence.restore.test.js pca render-cache case updated to the rebuild contract (Rule 4). TEST GAP that hid this (still worth closing generally): e2e/stats.restore-roundtrip.spec.js omitted pca and calls loadFromPayload() directly (bypasses archive serialize + render-cache restore + activation ordering); the prior unit cases asserted only report-panel text and fed the LIVE captureRenderCache() object straight back (never through serialize/deserialize); e2e/reopen.redraw-on-data-change.spec.js only checks the graph signature changes on edit; jsdom can't measure geometry. GENERALIZE (still open): audit roc/survival/heatmap/surface/scatter/line — any component whose captureRenderCache snapshots stats DOM and whose render functions hold module-level node refs has the same orphan-on-archive-restore exposure; converge them on "render cache = graph only, stats rebuilt from state" and a shared reopen/recovery stats-presence contract test. Relates to P0 #6 "Stats restore authority is inconsistent".

[open][P1][2026-06-03] Cross-component stats reopen audit (follow-up to the PCA fix GENERALIZE note). Static + empirical (real-browser richness-diff before vs after archive reopen of every stats component) findings: (1) BOX loses its stats SVGs on reopen/recovery/tab-switch — assumption QQ-sparklines (svg.assumption-sparkline--qq, ~3 svgs / ~36 paths) vanish while tables+text survive. ROOT CAUSE: box rebuilds its stats surface from the saved panel model via Shared.stats reporting.restorePanelModel, but capturePanelModel (js/shared/stats.js captureStatsNodeModel ~1926) serializes only a tag WHITELIST (STATS_MODEL_ALLOWED_TAGS ~1908: div/p/span/strong/em/i/b/small/table/thead/tbody/tfoot/tr/th/td/caption/ul/ol/li/details/summary/pre/br — NO svg); any non-whitelisted tag is reduced to textContent, and <svg> has none, so the sparklines are dropped at capture and never restored. Box is the only component that renders SVG INTO the stats RESULTS panel, so it is the only visible content-loss. RESOLVED for box via option (A) (homogeneous with the PCA fix, no shared-model widening): serializeAssumptions now persists qqPoints (+ parametricOverrideActive); renderAssumptionSection reads both the live (group.normality) and serialized (flat) shapes; hydrateBoxStatsSurfaceFromTabPayload rebuilds the assumption section (sparklines included) from the persisted diagnostics data after restorePanelModel. (2) CONVERGENCE of the replay components — empirically classified each via the reopen contract: scatter/line/pie RESOLVED (their loadFromPayload already rebuilds stats from the persisted payload, so the render-cache stats replay was pure redundancy — removed; captureRenderCache/restoreRenderCache now carry the graph only, matching box/pca). hist/roc/survival/surface NOT converged: their loadFromPayload does NOT rebuild the stats panel on the skipDraw reopen path, so the render-cache stats replay is LOAD-BEARING (removing it blanks their stats — proven by the contract). Their replay works (table/text only, no SVG, re-resolved live containers, no orphan-aliasing), so there is no active bug; full convergence requires first giving each a "rebuild stats from persisted state on load" path (like scatter/line/pie/box/pca) — tracked as the remaining follow-up. (3) heatmap: kept (replay + model-rebuild fallback, guarded by e2e/heatmap.reopen-recovery.geometry.spec.js). venn: kept (its analysis replay carries external GO/STRING results that must not be re-fetched on restore). GUARD: e2e/stats.reopen-presence.contract.spec.js asserts no stats richness loss (svg/rows/vector/text) after archive reopen for box/scatter/line/pie/hist/roc/survival/heatmap/surface (+ box crash recovery); pca has its own e2e/pca.stats-restore.spec.js. Relates to P0 #6 "Stats restore authority is inconsistent".

[open][P1][2026-06-03 RESOLVED] Stats-table Download/Copy export controls were broken on reopen/recovery in every component with a stats section. Two manifestations, both fixed: (1) ABSENT/raw-text (scatter/box/line/pie + any panel-model component): the controls are .export-dropdown widgets built by Shared.statsTable.render -> Shared.exporter.mountSvgStringControls, persisted via capturePanelModel; the stats-model tag whitelist (js/shared/stats.js STATS_MODEL_ALLOWED_TAGS) excludes <button>, so on restore the menu items were serialized to plain text (rendered as "PNGSVGEMFPDFTIFFCSVExcel...") with no working control. (2) PRESENT-but-DEAD (roc/hist/survival): components that restore stats via serialized render-cache DOM replay got the dropdown markup back but with no JS listeners (clicking did nothing). ROOT CAUSE (shared): interactive export controls cannot survive DOM serialization; they must be re-mounted from data on restore. FIX (js/shared/stats-table.js + js/shared/stats.js, homogeneous "rebuild from state"): statsTable.render now stashes the plain table data model on the card (wrapper.__statsTableModel) plus data-stats-export-filename/context attributes; capturePanelModel captures stats cards as a {type:'stats-table', model} node (not the mangled DOM) and renderStatsNodeModel re-renders the card via statsTable.render on restore (live controls) — fixes manifestation (1) centrally for all panel-model components with zero per-component changes. New Shared.statsTable.rehydrateExportControls(root) re-mounts controls for replayed cards using the stashed model when present else reconstructing the model from the table DOM (columns/rows/caption/footnotes + persisted filename attrs); called from roc/hist/survival/surface restoreRenderCache after the stats DOM is replayed — fixes manifestation (2). VERIFIED LIVE (menu opens, 7 items) after archive reopen for scatter/box/line/pie/roc/hist/survival; surface has no stats-table export controls (no-op). GUARD: e2e/stats.reopen-presence.contract.spec.js now also asserts export-dropdown count is preserved AND the restored control's menu opens (expectExportControlsLive) for every component on reopen (+ box recovery). Note: roc/hist/survival still restore stats via render-cache replay (the deferred convergence below) — rehydrateExportControls makes their controls live without requiring that larger refactor.

[open][P1][2026-06-03 RESOLVED] Three venn restore/redraw defects, all fixed in js/components/venn.js by normalizing to the sibling-component pattern (rebuild derived state from data; route table mutations through a real draw callback): (1) GO ANALYSIS CHART blank/missing — renderGOChart sized the canvas from canvas.offsetWidth, which is 0 when the GO results panel is hidden (e.g. a re-render triggered while the STRING tab is active, or a colour-scheme change), producing a 0-width chart; switching to the GO tab never re-rendered it, and reopen restored only a bitmap. FIX: renderGOChart now falls back to clientWidth/parent width when offsetWidth is 0, and updateAnalysisResultsVisibility re-renders the GO chart from state.analysis.lastGOResult (which IS persisted in payload.analysis.goResult) when the GO panel becomes visible. (2) OVERLAP-GROUP SWITCHING did not refresh the gene list after reopen — populateRegion/getRegionText read state.analysis.lastRegions, which capture nulls (Set/Map don't survive JSON) and restore did not rebuild. FIX: new ensureVennAnalysisStateFromData()/ensureVennRegionsForLookup() rebuild regions + counts from the restored gene-list inputs on demand (used by populateRegion/getRegionText and by restoreRenderCache, which also fixes the count summary showing 0 after recovery). (3) UNDO did not redraw the diagram while a direct delete did — venn passed a no-op `() => {}` as the table draw callback to Shared.hot.createStandardTable, whereas scatter/box/line/hist pass a real draw proxy; the hot wrapper invokes that callback for undo/redo/fill/structural mutations (triggerSchedule -> scheduleFn), so a no-op meant only the afterChange hook (direct edits) redrew. FIX: pass scheduleVennTableDraw (syncs inputs from table + redraws, skipping programmatic loadData). GUARD: e2e/venn.restore-recovery.spec.js (GO chart on tab switch + reopen; overlap-group switching after reopen; undo redraw). General rule reaffirmed: render cache restores DOM only; derived analysis state must be rebuilt from data on restore, and all table-mutation paths must redraw via the component's draw proxy, not just afterChange.

[open][P2][2026-06-03] Pre-existing (NOT caused by the PCA stats fix above; reproduced on committed HEAD with pca.js reverted): e2e/stats.archive-reopen-tab-switch.persistence.spec.js fails the two `line` cases ("archive reopen keeps line statistics after tab switch", "recovery restore keeps line statistics after tab switch"). The failure is in setup (waitForStatsReady): after clicking #lineComputeStats the status stays "Statistics unavailable until data is loaded." and never reaches "Statistics up to date." — i.e. the line example data has not finished loading when compute is clicked, so the tab-switch/reopen assertions are never reached. Reproduces consistently in isolation (`-g line`). Likely a line example-data load/compute race (or insufficient wait) specific to this spec's heavy flow; the other 18 component cases (incl. both pca cases) pass. Decide: harden the line wait in the spec (wait for grid data before compute) or fix a genuine line afterLoadData→compute-enable race. Possibly related to the 2026-06-03 bulk-loadData payload-sync entry (line ended clean+stale-empty there too).

[open][P1][2026-06-04] Long graph/stat render work lacks a shared cooperative cancellation and responsiveness contract. ROOT CAUSE: components mix passive loading overlays, per-component draw tokens, partial async-scope stale-result guards, worker offload for only some phases, and long synchronous SVG/canvas/data loops. A "Stop drawing" button alone would not reliably work because the main thread cannot process the click while inside a blocking loop, and current Shared.Workers.runTask can ignore stale results but cannot cancel/terminate a specific in-flight task. BIGGER REFACTOR: introduce a shared tab-scoped render/job controller that owns render tokens, AbortSignal-style cancellation, overlay stop/retry UI, worker task IDs with cancel/terminate support, chunk/yield helpers for large main-thread loops, frame-publication cleanup on cancel, and idle/busy state for save/recovery. Normalize all components to use it for graph drawing, stats, imports, analysis, and restore redraws, preserving true tab isolation. Add real-browser responsiveness/performance guards with large datasets that assert cancel is clickable, stale work cannot publish, retry redraws from the same payload, and unrelated tabs continue unaffected. Related but broader than the 2026-06-01 SVG frame publication lifecycle issue.

[open][P1][2026-06-04] Venn/Pie/Survival still do not participate in the shared render loading-overlay contract used by scatter/box/pca/line/heatmap/hist/roc/surface. ROOT CAUSE: their draw paths predate the standardized Shared.loadingOverlay.createPendingController integration, so the new Shared.jobs stop/retry graph overlay cannot attach there without first adding equivalent overlay lifecycle wiring and cooperative checkpoints. Normalize these components onto the same render-job controller, with tab-scoped cancelCurrentDraw hooks and large-dataset responsiveness guards, so graph cancellation truly covers every component homogeneously.

[open][P2][2026-06-04] Desktop/native menu import still depends on component toolbar element IDs. Open/Save/Save As now route through the tab-scoped Main.desktopCommands dispatcher and workspace-level sessionActions + Shared.fileIO desktop IPC, but Import and Load Example are still bridged by resolving the active tab's mounted root and clicking the existing component toolbar controls. BIGGER REFACTOR: each component should expose homogeneous importExample/importData commands through its lifecycle/registry, then Main.desktopCommands can invoke those APIs directly instead of relying on per-component button IDs. This keeps native menus desktop-only, preserves true tab isolation, and removes the last DOM-command coupling from desktop menu file actions.

[open][P2][2026-06-04] Table import supported-format metadata is duplicated between hidden file input accept attributes, shared toolbar tooltip text, and tableImport.openFile extension dispatch. The PZFX parser was implemented, but the toolbar affordance lagged behind PRISM because there is no shared import-format registry. Introduce a single Shared.tableImport supportedFormats contract that drives accept strings, tooltip text, parser dispatch, and tests across components.

[open][P2][2026-06-04] Component import/open/save control binding remains inconsistent. Table-import controls are now guarded with per-node deduplicating binders in Box/Scatter/PCA/Pie/Line/Hist/Heatmap/ROC/Survival/Surface, but the implementation is still duplicated per component and graph Open/Save/Save As controls still use mixed raw binding patterns. Normalize all file and command controls onto a shared deduplicating control-binding helper or component lifecycle command binding contract.

[open][P2][2026-06-04] colorSchemes active visual sync still infers preset/custom state by polling and comparing projected payloads. The large-matrix scan/clone hot path was fixed by compacting color-comparison payloads and bounding data-key scans, but the architecture remains fragile: palette UI state should be driven by explicit per-component color-state signatures emitted on payload/style changes, not a 350ms monitor that derives state from payload contents. This would avoid recurring large-data regressions and make color intent independent from raw table data cardinality.

[open][P1][2026-06-04] Scatter large-point rendering still mixes data collection, coordinate geometry, style bucketing, canvas rasterization, SVG assembly, and persistence/cache publication in one main-thread draw. The background color/formula loops were fixed, and the narrow density/uniform canvas renderer now has an indexed-sprite path that avoids per-point canvas point objects while preserving the same marker sprite/DPR/density-bin output. Remaining work: refactor scatter toward a state-first render model with reusable typed coordinate/style buffers, optional OffscreenCanvas worker rasterization, and idle-deferred render-cache/session persistence. Preserve exact visible/exported output by reusing the same coordinate transform, marker sprite generation, DPR, density quantization, and cache metadata; keep headed-browser performance guards for test-scatter.csv.

[open][P2][2026-06-05 RESOLVED for shared-overlay components] Graph loading-overlay heaviness was not normalized across box/pca/line/hist/heatmap/roc/surface, so empty tab creation could still trigger forced graph overlays even when no body data existed. Fixed by adding a shared table render-cost predicate to Shared.loadingOverlay and wiring it into those components; scatter already uses its canvas threshold. Guard: e2e/graph-loading-overlay.empty-tabs.spec.js opens fresh empty scatter/box/line/pca/hist/heatmap/roc/surface tabs and asserts no visible graph overlay. Remaining broader gap is tracked by the 2026-06-04 Venn/Pie/Survival shared render loading-overlay contract issue.

[open][P1][2026-06-05] Archive/recovery render-cache restores lack a shared "live interaction hydration" contract. ROOT CAUSE: domControls accepts archive-backed render caches as authoritative, applies payload with skipDraw/skipInitialDraw, restores serialized graph fragments, and suppresses follow-up draws; but serialized SVG/DOM nodes do not preserve per-node JS listeners, closure state, WeakMap registrations, or component-private render models. Components therefore reopen with visually correct but behaviorally stale graphs: box restored plot nodes can miss point/shape/axis/font toolbar bindings, while heatmap palette edits can update state but be swallowed by restore draw suppression or operate without a rebuilt render model. BIGGER REFACTOR: define one restore contract for graph interactivity: either (A) render cache is visual preview only and every activated reopened tab immediately performs a real draw from restored payload before accepting graph edits, or (B) each component exposes an explicit hydrateRestoredGraphInteractions/cacheHydrate hook that re-registers fontControls, axisControls, symbol/shape handlers, 3D/drag handlers, render models, and palette/legend bindings after restore. The contract must clear post-restore suppression on user edit, validate that restored graphs are "live", and include real-browser reopen/recovery guards for toolbar visibility plus style/color changes across scatter, box, heatmap, line, and other SVG-heavy components. Scatter currently behaves closer to the intended model because activation reapplies payload-derived visual/runtime state and its graph-only cache is less dependent on serialized stats/interaction DOM; use it as the normalization reference but do not assume its cache restore is fully certified without these guards.

[open][P1][2026-06-05] User style/control edits are not normalized onto a synchronous payload write-through contract, so recovery can persist stale graph styling. ROOT CAUSE: many toolbar callbacks mutate component-local state and redraw (e.g. box point/shape style maps, heatmap palette/value-scale state) while relying on global dirty tracking plus later live `getPayload()` reconciliation; manual `.graph` save is save-like and forces that reconciliation, but recovery/autosave use lean lifecycle snapshot policies that may preserve cached `tab.payload` when the tab appears already flushed or clean. BIGGER REFACTOR: define one component mutation API for payload-affecting visual edits that atomically updates local state, updates or invalidates `tab.payload`, marks the owning tab dirty, clears stale render caches, and schedules recovery from the new payload signature. Replace ad hoc state-only callbacks in symbol/color/palette/axis/font toolbars with that API, and add real-browser recovery guards that edit box symbols and heatmap colors, wait for recovery, reload, accept recovery, and assert payload + rendered color/shape parity. Related to the state-first snapshot accessor P0 and archive/recovery live interaction hydration issue, but distinct because the data loss happens before restore: the recovery archive can be stale even when reopen of an explicitly saved file is correct.
