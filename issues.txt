[closed 2026-05-17][fixed] Cross-component activation/theming reconciliation was unified on lifecycle-driven sync for active tabs (render-cache-restored + draw-executed), with publication-style and color-scheme controls now participating in the same post-activation reconcile flow.
[closed 2026-05-17][fixed] Render-cache restore no longer risks stale interactive runtime on ordinary per-tab activation: runtime (non-archive) render-cache replay is blocked for per-tab DOM workspaces and falls back to normal activation/draw paths.
[closed 2026-05-17][no-longer-relevant] Tab isolation concerns tied to singleton drift are superseded by per-tab mounted roots + runtime snapshot bridges + activation/session reconciliation already present in the shared lifecycle/workspaceTabs stack.
[closed 2026-05-17][fixed] Aspect-lock default/parse/apply semantics were centralized in `Shared.aspectLock` and consumed by `resizer`, `componentLayout`, `graphSizing`, and `chartStyle`.
[closed 2026-05-17][fixed] Shared activation contract now avoids early-return starvation on DOM rebind: `bindTabActivation` no longer exits before `afterReady`, and supports shared render-readiness enforcement.
[closed 2026-05-17][no-longer-relevant] Table access has converged on tab-scoped `ensureHotForActiveTab`/`ensureTableForTab` patterns across components; remaining direct `state.hot` reads are local aliases behind tab-scoped acquisition paths.
[closed 2026-05-17][no-longer-relevant] Clean-tab payload drift concern is superseded by current payload/layout signature governance and lifecycle snapshot normalization in session + component lifecycle flows.
[closed 2026-05-17][fixed] Runtime mutator root resolution duplication was reduced by introducing `Shared.workspaceTabs.resolveTabScopedRoot` and migrating shared mutator modules (`colorSchemes`, `publicationStyles`) to this shared resolver.
[closed 2026-05-17][no-longer-relevant] Hardcoded `chartStyle.TEXT_COLOR` fallbacks now act as baseline defaults while active theme application is handled by shared theme/lifecycle pipelines; this is no longer a tab-leak driver.
[closed 2026-05-17][no-longer-relevant] Redundant theme token persistence is currently intentional for reopen fidelity and user-overridable state replay; not treated as an active defect.
[closed 2026-05-17][no-longer-relevant] Control discovery risk from global IDs is mitigated by mounted-root resolution + active DOM rebinding guarantees in shared workspace/lifecycle infrastructure.
[closed 2026-05-17][no-longer-relevant] Runtime snapshot schema inconsistency is superseded by shared lifecycle internal-state bridging and workspace runtime snapshot routing.
[closed 2026-05-17][no-longer-relevant] Scatter warm-switch cache rejection observation is no longer reproducible with current cache validation + activation flow.
[closed 2026-05-17][no-longer-relevant] Resizer-option relocation divergence is superseded by centralized `.resizer-options-menu` behavior in shared `resizer`.
[closed 2026-05-17][no-longer-relevant] Venn warm-switch cache rejection observation is no longer reproducible with current lifecycle/cache restore contract.
[closed 2026-05-17][fixed] Shared resize lifecycle start-phase hooks are present and used (`componentLayout` resize-start invalidation + shared resizer lifecycle callbacks).
[closed 2026-05-17][fixed] `Shared.hot` undo tab-context resolution no longer references an out-of-scope wrapper symbol; wrapper resolution is now explicit and local-safe.
[closed 2026-05-17][fixed] `Shared.ensureGraphViewport` no longer hardcodes `preserveAspectRatio: xMidYMid meet` as an implicit default; callers must pass explicit preserve mode, with neutral default behavior retained in `autoResizeSvg`.
[closed 2026-05-17][fixed] Heatmap draw scheduler merge no longer leaks stale `viewOnly=true` across unrelated actions; omitted `viewOnly` now resolves to full redraw intent.
[closed 2026-05-17][fixed] Aspect-lock state authority now resolves through shared `Shared.aspectLock` with live dataset precedence and consistent graph/resizer lock propagation.
[closed 2026-05-17][fixed] Aspect-lock behavior drift risk was reduced by centralizing lock parsing/application authority and validating subtype lock enforcement across line/scatter/pca/venn/surface/heatmap e2e flows.
[open 2026-05-18] Line/scatter SVG theme application is duplicated across 2D draw, 3D draw, and preview clone paths; this drift caused a scope regression (lineThemeDark undefined). Refactor into one shared helper (e.g., Shared.colorSchemes.applySvgThemeMetadata) consumed by all components and preview builders to prevent recurrence.
[open 2026-05-18] `chartStyle.computeResizeScale` has one global unlocked-resize typography policy (stable text/margins via `resizerUnlockedStyleScaleBase`) that does not fit every chart type; components that need proportional unlocked scaling (e.g., pie/radial layouts) must currently patch locally. Introduce an explicit shared policy flag (`stable` vs `proportional`) so components can opt in without duplicating scale logic.
[open 2026-05-18] `chartStyle.applySvgDefaults` mixes visual defaults with interactive `fontControls.enableForSvg` attachment, forcing components to bypass the helper in resize-preview paths to avoid per-frame editor rebind overhead. Split this into separate helpers (pure SVG styling vs interactive wiring) and standardize component usage.
[open 2026-05-18] Viewport-fit sequencing is inconsistent across components (some call ensureGraphViewport before full scene composition while others call it at end), and resize uses mixed remeasure policies; this can produce chart jitter and post-release jumps. Introduce a shared post-draw viewport finalization contract (single fit point + phase-aware remeasure policy) to normalize behavior.
[open 2026-05-18] Bottom-axis label reserve/rotation behavior is duplicated across components (e.g., box vs pie stacked) with per-component thresholds/options, which caused clipping drift. Introduce a shared chartStyle helper or contract for rotated x-tick reserve + hysteresis so all cartesian-like modes stay aligned.
[open 2026-05-18] Orthogonal viewport lock currently freezes the non-resized viewBox axis too strictly during live drag, so late-appearing geometry (e.g., rotated tick labels) can be clipped. Standardize lock semantics to allow monotonic overflow expansion on the frozen axis while still blocking shrink jitter.
[open 2026-05-18] Box strip large-dataset resize path checks canvas reuse too late (after expensive swarm/layout prep), unlike scatter's early move-phase reuse gate; this causes jerky live resize and component behavior drift. Introduce a shared phase-aware point-layer resize contract (live SVG recompute + reused canvas during move, recompute canvas on resize end).
[open 2026-05-18] Components are split between generic `ensureGraphViewport` (autoResizeSvg, preserveAspectRatio=`none`) and bespoke aspect-preserving viewport lockers (e.g., box), which causes cross-component rendering drift (text stretch vs preserved glyph aspect) under resize. Introduce one shared viewport-fit mode contract (`fill-distort` vs `fit-preserve-base-aspect`) so components do not fork viewport geometry logic.
[open 2026-05-18] `chartStyle.computeBottomLayout` always includes x-axis title reserve in its baseline offset; components without an x-axis title (e.g., pie stacked) must subtract/override this locally, causing inconsistent bottom-reserve behavior. Add an explicit `hasAxisTitle`/`axisTitleReservePx` option and migrate components to the shared contract.
[open 2026-05-18] Box stats availability is still timing-coupled to draw sequencing: in significance layout e2e (box.significance-layout test #405), #boxStatsStatus can remain 'Statistics unavailable until data is loaded.' after example data load. Decouple stats-context readiness from SVG frame commit timing and initialize it directly from trace-collection completion to avoid draw-phase races.
[open 2026-05-19] Scatter/box still couple payload serialization and some control wiring to live DOM nodes instead of tab state snapshots, which makes headless/inactive-tab flows fragile and caused recent null-path regressions. Introduce a shared state-first control accessor contract (DOM optional, state authoritative) and migrate component payload builders + init listeners to it.
[open 2026-05-19] Test suites still duplicate ad-hoc workspace bootstrap (mounted roots, active-tab tokens, toolbar/data-view wiring) instead of using one shared tab-scoped harness; this causes frequent false regressions when lifecycle/cache contracts evolve. Introduce a canonical test bootstrap utility that provisions workspaceTabs/session ownership tokens + required shared modules per component, and migrate legacy suites to it.
[open 2026-05-19] Full-app integration tests (booting js/main.js + tabs stack) still rely on implicit session/tab bootstrap order and conflict with shared harness pre-bootstrap; this makes suite setup fragile (e.g., workspace-NaN tab IDs when baseline session is preseeded). Define a deterministic bootstrap contract for integration suites (harness mode: none vs preseeded) and enforce it in one helper.

[closed 2026-05-19][fixed] __tests__/hot.aggrid.clipboard-selection.test.js leaked cross-test state (live table instances, document listeners, pending undo context) because tables were not deterministically destroyed between tests; suite now uses centralized table tracking + teardown (destroy + DOM reset) for isolation.
[open 2026-05-19] AG Grid native column-move tests still use bespoke columnApi stubs in multiple files; when stubs do not model setColumnDefs resetting displayed-order state, undo paths can recurse and create false failures. Introduce one shared AG Grid column-order mock factory/helper with explicit identity-reset semantics to keep undo/redo tests faithful and reduce fixture drift.
[open 2026-05-21] Box flip/unflip still depends on two competing frame-size authorities (`.svgbox` resizer datasets vs plot drawing-zone measurement during draw); transient stale zone sizes can desync viewBox scaling and axis spans during orientation swaps. Refactor to a single frame/viewport authority contract (authoritative frame snapshot -> deterministic inner plot zone resolver -> draw) shared by resizer + draw pipeline to eliminate orientation race conditions.
[open 2026-05-21] Box flip-transition reserve handling is still split across ad-hoc runtime flags (`flipAxisSpanTarget`, `flipHorizontalReserveCarryoverPx`, viewport-extension auto-resize guards) and draw-reason heuristics; this makes orientation invariants fragile when significance/reserve states change mid-transition. Introduce one explicit flip-transition state machine (capture -> transpose -> reserve-transfer -> settle -> release) so axis-span locking, reserve transfer, and resize-observer muting are coordinated deterministically.
[closed 2026-05-22][fixed] Box flip-transition reserve/lock handling was consolidated into an explicit state machine (`state.flipTransition`) with orientation snapshots (`vertical`/`horizontal`), deterministic pending transfer state (axis-span target, draw-zone override, reserve carryover), transition lifecycle (`start`/`settle`), and runtime snapshot persistence/restore; flip/unflip now routes through one orchestrated capture->transpose->reserve-transfer->settle path.

[open 2026-05-22] Box reserve auto-resize still uses asymmetric authority-promotion rules (vertical persistGraphFrameAuthority vs horizontal promoteGraphDatasetAuthority heuristics), which makes stale frame reapply regressions easy to reintroduce when new reserve flows are added. Refactor to one shared reserve-resize commit contract that persists resolved base dimensions from the resizer result for both axes under a unified transition-aware policy.
[open 2026-05-22] Box stats restore authority is split between payload-apply and activation-time surface hydration: signature-sensitive runtime fields (e.g., selected comparison indices) can diverge from persisted stats metadata after archive restore, causing false context invalidation on later tab activation. Consolidate stats restore into one canonical hydration step (payload + runtime signature inputs together) before any context priming so activation cannot recompute against partially restored stats state.
[open 2026-05-22] Stats restoration semantics under render-cache activation are inconsistent across components: when domControls applies payloads with skipDraw/suppressStatsRecompute, components without an explicit payload-time stats adoption step can lose restored status/results fidelity (observed in pie reopen+tab-switch). Define one shared stats-restore contract for payload apply vs draw suppression (hydrate results/status/signatures from saved stats, then prime context deterministically once data bind completes).
[open 2026-05-23] Archive render-cache canvas bitmap rehydration is duplicated across scatter/box (and partially split from preview/session serialization logic), which risks future drift between reopen/recovery fidelity and tab-preview fidelity. Introduce one shared render-cache canvas bitmap hydration helper/contract consumed by component restore + preview rebuild paths.
[closed 2026-05-24][fixed] Box resize-zone synchronization now measures the resizer zoom viewport as the canonical drawing frame when available, so `__tests__/box.swarmOffsets.test.js` no longer falls back to stale plot client height during vertical growth after shrink.
[open 2026-05-24] Core runtime modules remain professional-grade blockers by size and responsibility density (`js/components/box.js` ~36.8k lines, `scatter.js` ~23.4k, `js/shared/hot.js` ~16.2k, `line.js` ~13.9k, `pca.js` ~11.9k). Split these into tested domain modules (state/payload, controls, draw geometry, stats, persistence, render cache) before further feature work, otherwise every bug fix will keep touching monolith-local incidental state.
[open 2026-05-24] Unused scaffold modules (`js/app.js`, `js/ui.js`, `js/utils.js`) are not loaded by `index.html`, are barely referenced, and mix ESM exports with the IIFE/CommonJS runtime style used elsewhere. Remove them or migrate them into the real bootstrap path; keeping dead alternate entrypoints makes package contents and coverage policy misleading.
[open 2026-05-24] Component-contract documentation is not source-derived enough to be trustworthy: `scripts/generate-component-contracts.js` hardcodes payload baselines, already omits persisted `stats` for heatmap/surface relative to their live `getPayload` implementations and `state-persistence-schema.md`, and stale generated line references are not checked in CI. Make contract generation introspect or validate live payload factories and fail CI on doc drift.
[open 2026-05-24] The quality gate is still mostly test-only: there is no lint/format/type/static-check script, no CI-enforced architecture/doc-generation check, and Jest global coverage thresholds are very low (25% statements/functions, 15% branches). Add explicit quality lanes before calling the codebase professional-grade.
[open 2026-05-24] Persisted statistics/report UI still round-trips executable markup (`resultsHtml`/`reportHtml`) through `innerHTML` in shared stats restoration and component fallbacks. Replace saved stats HTML with structured report models plus a single renderer/sanitizer so archive restore, previews, and stats panels do not depend on trusted markup blobs.
[open 2026-05-24] User-authored data transforms compile expressions with `new Function` after regex identifier replacement. Replace this with a small expression parser/AST evaluator so transform logic has a well-defined grammar, better errors, and no runtime code-generation trust boundary.
[open 2026-05-24] Browser runtime still depends on CDN-hosted core libraries in `index.html` while tests patch those URLs to local node_modules. Bundle or consistently vend these dependencies for production/web/desktop parity, and keep SRI/cache policy as a release artifact instead of a hand-maintained HTML concern.
[open 2026-05-24] Lifecycle contract installation is fail-soft: `installComponentLifecycleDefaults` adds no-op/null `captureRuntimeState`, `applyRuntimeState`, `captureUiState`, and `applyUiState` methods, which lets components appear lifecycle-capable even when they do not preserve meaningful runtime state. Split capability declaration from fallback behavior and add contract tests that require non-empty/state-relevant capture for components that claim warm-switch/reopen fidelity.
[open 2026-05-24] Tab-scoped async safety is fail-open when workspace session metadata is missing or has no positive generation: `workspaceTabs.isSessionMetaCurrent` returns true for absent/invalid metadata. Convert async/draw/stat scheduling to require current session metadata on tab-scoped paths, with explicit opt-out only for proven synchronous/local callers, so stale callbacks cannot silently bypass tab isolation.
[closed 2026-05-24][fixed] Draft owned-runtime refactor stored per-tab runtime in component module-scope maps (`lineOwnedRuntimeByTab`, `scatterOwnedRuntimeByTab`, `pcaOwnedRuntimeByTab`, `boxOwnedRuntimeByTab`, plus lightweight maps in smaller components) without an explicit tab-dispose/delete path; component-owned runtime now lives under shared workspace/session runtime ownership, `__global__` owned-runtime fallbacks were removed, and `createRuntimeOwner.dispose` clears normalized snapshots plus `ownedRuntimeRecord`.
[closed 2026-05-25][fixed] Local component async guards remained heterogeneous after the owned-runtime refactor. `Shared.componentLifecycle` now exposes scoped component frame/timeout schedulers backed by `createAsyncScope`, Venn species detection and tab-sensitive Box/Scatter/PCA/Line/Heatmap/Histogram/Pie/ROC/Surface/Survival deferred work route through lifecycle async scope, and direct component tests now provide explicit tab ownership instead of relying on ambient active-tab state. Remaining native timers are limited to non-runtime browser cleanup, internal debounce implementation details, or local UI retry loops that do not persist durable runtime.
[closed 2026-05-24][fixed] Box no longer has an `authoritativeLayoutInPayload` registration or graphSizing/session hard-coded skip path; the now-unused authoritative-layout-in-payload policy hook was removed, and Box participates in the same layout-state + payload graphSizing enrichment/merge contract as other components, with drawable-frame authority centralized through `Shared.componentLayout.resolveDrawableFrame`.
[closed 2026-05-24][fixed] Histogram draw sizing no longer treats `#histPlot.clientWidth/clientHeight` as drawable-frame authority; Histogram now resolves render dimensions through `Shared.componentLayout.resolveDrawableFrame`, with regression coverage proving the resizer zoom viewport or `.svgbox` beats stale plot output.
[closed 2026-05-24][fixed] Drawable-frame authority is now normalized across the remaining renderers (`venn`, `line`, `scatter`, `pca`, `pie`, `roc`, `survival`, `heatmap`, `surface`): render sizing and font scaling route through `Shared.componentLayout.resolveDrawableFrame`, direct plot-client sizing was removed, and cross-component regression coverage asserts that the resizer zoom viewport beats stale plot output.
[closed 2026-05-24][fixed] Component module-scope `Main?.` references in `venn`, `pca`, and `roc` were replaced with `global.Main?.`, removing hidden bootstrap-only assumptions exposed by isolated component contract tests.
[closed 2026-05-24][fixed] Scatter tab previews with dense SVG marker layers now honor the documented >400-circle simplification threshold instead of waiting for the generic >7000-node fallback, so previews down-sample real circles while preserving spatial structure.
[closed 2026-05-25][fixed] Runtime ownership still wrote compatibility snapshots to multiple shared/session locations (`componentRuntimeSnapshot`, workspaceTabs runtime snapshots, and session `ownedRuntimeRecord`). Runtime ownership now resolves through one `runtime.lifecycle` schema (`snapshot` for durable capture, `ownedRecord` for live component-owned runtime), legacy `sharedState.runtime[__workspaceTabs__:type]` snapshots migrate on read, and component code uses named workspaceTabs helpers instead of top-level runtime fields.
[closed 2026-05-25][fixed] Owned-runtime bindings risked creating default per-tab records during activation/capture/deactivate/apply paths instead of only binding existing hydrated records or records derived from an actual runtime snapshot. Bind paths now use non-creating lookups, snapshot apply/remember are the only component-owned record creation points, PCA/Scatter activation seed writes were removed, and lifecycle tests prove owned-record lookup is not a hidden create operation.
[closed 2026-05-25][fixed] Runtime snapshots now have an enforced transient-work contract. `Shared.componentLifecycle.sanitizeRuntimeSnapshot` removes pending draw/stat/rotation fields, draw tokens, async state, workers, promises, controllers, signals, timers, RAF ids, and other in-flight handles before durable runtime storage. Runtime-owner capture and direct `workspaceTabs` runtime capture both store sanitized snapshots, and `workspaceTabs.captureRuntimeState` now returns the same sanitized object it stores. Contract tests prove transient fields are stripped without mutating the source snapshot and that workspace-level runtime capture cannot persist in-flight state.
[closed 2026-05-25][fixed] Several non-owned runtime initialization paths still resolved implicit active-tab or sentinel default IDs. `Shared.hot.resolveTableTabId` now requires explicit table ownership in strict/debug/test mode, workspace registry ensure calls propagate tab metadata, table-backed components no longer create `*-default` table IDs, direct component tests declare test tab IDs, and scans show no remaining targeted active-tab/default ownership fallbacks in component ensure/bootstrap paths.
[closed 2026-05-25][fixed] Runtime disposal is now routed through a single session/tab teardown contract: `Main.session.disposeWorkspaceTabResources` and `disposeWorkspaceTabs` centralize tab resource release for tab close, graph-selection reset, and session/archive replacement before the tab list is rewritten. `workspaceTabs.disposeTab` accepts an explicit component type for old-type teardown, cancels workspace/component async scopes, disposes AG Grid tables and runtime-owner lifecycle records, detaches mounted roots, and clears tab shared state. Contract coverage proves explicit type override disposal, async-scope invalidation, and session reload teardown; the full Jest baseline passes (`120` suites, `1024` tests).
[closed 2026-05-25][fixed] The Jest baseline was not reliably green as a single quality gate: the full `npm test -- --runInBand` run reached 119/120 passing suites but timed out `__tests__/box.statsControls.tabIsolation.test.js`, while the same suite passed alone in about 20 seconds. The Box stats isolation suite now uses controlled fake timers and explicit async draining instead of real-time sleeps; the full baseline now passes as one run (`120` suites, `1021` tests).
[open 2026-05-26] Strict tab-ownership enforcement currently leaks into UI/root-resolution helpers: `workspaceTabs.getSessionRecord` now requires an explicit resolvable tab object and throws in debug/test mode when passed a tab id not present in `session.workspaceState.tabs`. Callers such as `workspaceTabs.getMountedRoot`/`resolveComponentRoot` and `Shared.hot.ensureTableForTab` invoke this during component `ensure(...)`, which breaks isolated/component-level setup and causes broad test failures (`pca.view`, `line.view`, `stats.ui.persistence.restore`) before runtime ownership logic is even exercised. Split lifecycle-runtime strictness from UI-boundary root lookup so unresolved tab IDs return null (or use explicit uiBoundary mode) instead of throwing.
[open 2026-05-26] Tab-scoped schedulers are now strict, but component entrypoints still do not consistently propagate tab metadata (`tabId` + session meta) through payload/example/import draw paths. As a result, `createTabScopedScheduler` rejects legitimate flows with `tab-scoped schedule requires explicit tab` (notably PCA and Scatter load/example paths in tests), and warm-switch/stat-restore behavior is no longer verifiable against a green baseline. Normalize component public APIs to accept/forward explicit tab context and add contract tests that fail when any schedule/draw path can execute without ownership metadata.
[open 2026-05-26] SVG frame publication timing is inconsistent across components: some draws append the new `<svg>` to the live plot root before token/cancellation guards and before scene composition completes. This allows stale or empty intermediate frames to become externally observable (tests/UI selectors can bind to a non-authoritative frame). Standardize a shared frame lifecycle contract (`build off-DOM -> verify token current -> publish -> commit`) with cancellation cleanup to eliminate partial-frame races.
[open 2026-05-26] `workspaceTabs.ensureActiveDomBindings` is easy to misuse: components that omit `getCurrentRoot` can trigger perpetual `missingCurrentRoot` rebinding, repeated `init()` calls, duplicate event listeners, and re-entrant draw/overlay races (observed in Heatmap; Histogram still uses sentinel-only binding). Harden the contract by requiring current-root reporting (or deriving it from the sentinel) and add a cross-component test that asserts a single `init()`/single listener registration per activation cycle.
