# Open Issues (Reviewed 2026-09-11)
# Actionable backlog only. Resolved, stale, duplicate, speculative, and metric-only entries are intentionally excluded.
# Ordered by descending engineering priority. Within a priority, tab isolation, durable data/reopen correctness, and recovery integrity precede transient UI, performance, test, and maintainability work.
# Priority: P0 = confirmed durable cross-tab/data-corruption risk; P1 = high-impact correctness/isolation/recovery root cause; P2 = medium correctness, certification, performance, or architecture risk; P3 = maintainability/test/tooling debt.
# The 2026-08-21 audit was source/static; later runtime evidence supplied by the user is explicitly identified in dated follow-up entries. Deterministic source/generated-asset checks are identified explicitly below.
# 2026-08-22 recheck: the former Raw-DataView, per-tab significance-threshold, delayed graph-sizing retargeting, and dynamic palette owner-write-through findings are source-resolved in the current tree and therefore removed from the actionable backlog. The palette recheck normalized Box, Line, Scatter, and Survival to exact-owner write-through instead of accepting component-local mirror mutation as persistence. The new exhaustive parameter-isolation suites still require local runtime certification.

[open][P1][2026-09-10] Box horizontal resizing still writes layout-derived `meta.graphSizing` into the durable payload. The serial Chromium `box.dual-tab.significance-resize.isolation` case reproduces this after removing the layout field: a layout-only resize changes the payload from the default sizing record to the resized width/aspect ratio while `payloadDirty` remains false. The same owner then restores a different workspace table split (`workspace.tableFraction` about `0.24685` before exit and `0.25160` after re-entry) in the canonical resize persistence contract, proving a second layout-authority split rather than a pixel-only test problem. The same split is now reproduced by the shared PCA `component.resize-exit-reenter.persistence` case (`tableFraction` about `0.240536` before exit and `0.245330` after re-entry), so the defect is not Box-specific. `js/shared/resizer.js:183-198` correctly routes ordinary resizing through layout capture, but `js/main/session.js:2335-2393,3433` then calls `js/shared/graphSizing.js:785-804` during canonical capture/persistence, duplicating layout state inside payload content. Separate canonical payload, session, and layout signatures; ordinary resize must update layout/runtime state without changing payload content or statistics, while an explicit aspect-lock or graph-sizing preference change remains separately defined and tested.

[open][P1][2026-09-10] Box manual archive/reopen loses the first horizontal-resize bottom reserve. The serial Chromium `box.horizontal-resize-axis` case restores a bottom extension of `0` instead of `53`. `js/components/box.js:12604+` intentionally zeros significance and label reserves for version-2 saved geometry and relies on a later draw to remeasure, but manual reopen enables the restored graph before that canonical reserve is available. Restore the canonical user frame, remeasure/apply the required reserve before interaction and assertion readiness, and keep derived reserve geometry out of durable authority. Add manual reopen, recovery, and resize parity coverage.

[open][P1][2026-09-10] Venn archive checkpoints can be blocked by optional species-recognition lifecycle work after a settled graph is visible. Both serial Chromium `recovery.primary-graph-publication` and `reopen.redraw-on-data-change` cases fail with the production checkpoint error that no settled Venn frame was published. `js/components/venn.js:838+` schedules owner-scoped species work and `js/components/venn.js:13816+` treats pending species timers/requests and related analysis state as not idle; `js/main/sessionActions.js:791+` refuses the archive when the component is not idle. Define readiness around the required graph publication separately from optional enrichment, while preserving owner/token checks and a canonical flush when derived species state later commits; manual save, recovery, and reopen must not be unavailable merely because enrichment is delayed. Cross-check the existing Venn welcome-persistence issue before closing either entry.

[open][P1][2026-09-10] Pie render-cache restore does not guarantee first-interaction readiness. The serial Chromium `reopen.graph-edit-cache-invalidation` case cannot open the stacked-axis font toolbar on the first click after archive reopen. `js/components/pie.js:8209+` reports only active resizing, while `js/components/pie.js:8254+` and `js/shared/componentLifecycle.js:4632+` split render-cache restore and interaction rebinding. Make Pie readiness include owner-scoped restore/rebind completion and return only after axis, inline, trace, and legend interactions are attached to the restored projection; preserve the canonical session payload and add a first-click-after-reopen regression.

[open][P1][2026-09-10] Venn owner rebinding is not stable under the production-ordered module bootstrap. The `venn.additionalTabOpen` Jest integration suite passes 21/21 with its legacy partial module list (54.73 s), but the same suite under the index-derived production loader passes 20/21 and times out at 238 s with `venn.__boundTabId` still on the prior tab. The affected path exercises `js/main/tabs.js` tab activation and `js/components/venn.js:2447-2503,13205-13260`, where the component must bind the new session before projecting or applying inactive-owner runtime. Reproduce with the real Chromium application path, identify whether the extra production modules or the Venn activation/rebind ordering changes ownership, then fix the shared owner handoff rather than restoring partial test bootstraps. Keep the suite blocked from production-loader migration until the owner assertion passes.

[open][P1][2026-09-11] Heatmap payload hydration does not reliably apply an explicit view and later same-type projection can overwrite an explicit palette. The Chromium parameter matrix mutates `config.view` from `corr-columns` to `values`, but after owner activation, `loadFromPayload`, and canonical persistence the owner payload, session state, and `#heatmapView` remain `corr-columns`; the same-type matrix reproduces this serially and also restores the expected `soft` scheme as `custom` in the visible control while the owner payload remains `soft`. The direct user-facing view-switch contract passes, so this is specifically an owner hydration/projection boundary, not a missing selector. Audit `js/components/heatmap.js:1944-2215,13040-13132,13299-13470` and the parameter harness's payload-application path. Apply controls to the resolved owner root/session before any active-mirror capture, and prevent stale palette mirrors from overwriting an inactive owner's state; retain the exact payload/session/DOM isolation assertions.

[open][P2][2026-08-21] Surface fast-rotation cache geometry is serializable and owner-correct but unnecessarily duplicates dense mesh coordinates. The rotation model stores a full `points` list plus face objects containing repeated vertex point objects, and `captureRenderCache()` clones that model in addition to canonical table data and the SVG cache. Replace face vertices with indexed connectivity or another compact deterministic serializable representation, rebuild transient rotation pools after restore, and add archive-size/memory plus first-rotation performance regressions for the largest supported grids without changing reopen fidelity or owner boundaries.

[open][P2][2026-09-11] Heatmap archive render-cache restore changes the canonical payload signature even without a user edit. The serial Chromium `render-cache.persistence-contract` case for the one-tab Heatmap save/reopen path fails both in the mixed 62-test wave and in an isolated rerun: the reopened `canonicalPayloadSignature` differs from the saved archive signature. The first stable difference is the saved `statsPanelModel.resultsModel` being `null` while the reopened payload contains a populated stats-panel model; the data, configuration, cache hit, owner, and layout contracts otherwise reach their assertions. Audit `js/components/heatmap.js:13129-13175` (payload capture writes the live stats panel), `13383-13401` (payload hydration and cache restore), and `15159-15175` (render-cache stats projection). Make saved/reopened Heatmap state deterministic: either restore the exact durable stats model before capture or prevent an implicit post-restore stats projection from mutating canonical state. Preserve owner-scoped cache hits and existing statistical results; add a serial archive/reopen signature regression and compare save, cache-hit, and recovery paths before changing the assertion.

[open][P2][2026-09-11] Heatmap archive render-cache validation rejects a complete values-view cache and forces a redraw on cold reopen. The Chromium-only standalone parity run (`__tests__/tab-isolation-regression/artifacts/2026-09-11T02-23-47-727Z`) had a matching archive cache, payload signature, layout signature, and complete render metadata for both tabs, but the values tab's component validator rejected the cache and `domControls` forced a payload redraw; the sibling correlation tab reused its cache. This is a separate cache-completeness/readiness symptom from the Heatmap payload-signature drift above. Audit `js/components/heatmap.js:14841-14862,14980-14997` and the archive fragment deserialization path. Make a cache marked complete restore consistently across Heatmap view modes, or emit a precise rejection reason when redraw is required; retain owner, matrix-layer, interaction, and no-unnecessary-redraw assertions.

[open][P2][2026-09-11] ROC archive render-cache restore changes the canonical payload signature without a user edit. The Chromium `render-cache.persistence-contract` case fails in the four-worker run and again in an isolated one-worker rerun for `roc: one-tab save and reopen use the archive render cache`; cache readiness and reopen-phase assertions pass, but the reopened `canonicalPayloadSignature` differs from the saved archive signature. Audit `js/components/roc.js` together with the ROC stats-panel/cache hydration boundary to find which derived result or projection is written after archive capture. Make save, cache-hit, and reopen state deterministic while preserving owner-scoped metrics and render-cache reuse; keep the serial signature assertion unchanged.

[open][P2][2026-09-11] Pie same-component archive/cache restore changes the canonical layout signature without a user edit. The `render-cache.persistence-contract` same-component two-tab case reproduces serially and in the four-worker slice: the saved and restored Pie payloads agree, but the restored workspace `tableFraction` changes (approximately `0.3417` to `0.3587`). The one-tab case failed only in the parallel slice and passed in an isolated one-worker rerun, so that part remains parallel-sensitivity evidence rather than a confirmed serial defect. Audit Pie's stacked-axis layout capture/restore and shared workspace layout projection; preserve owner-scoped cache reuse and distinguish durable user layout from derived responsive measurement before changing the assertion.

[open][P2][2026-09-11] Shared Cartesian resize publication shifts the axis anchor independently of the saved graph size. Serial Chromium Box and Scatter pointer-resize contracts move the y-axis by about 10.4–10.5 px despite the expected stable anchor; Line archive/reopen horizontal-resize moves the corresponding page X by about 117.6 px. The failures survive one-worker execution and the resize tests now wait for owner/layout publication, so they are not parallel-only timing noise. Audit `js/shared/resizer.js`, `js/shared/componentLayout.js`, `js/shared/chartStyle.js`, and each Cartesian caller for mixed frame/workspace/envelope coordinates; establish one canonical axis/frame coordinate contract and retain cross-component resize, tab-switch, and reopen assertions.

[open][P2][2026-09-11] Zoom-only wrapper scaling changes Cartesian SVG viewBox geometry without a draw. Serial Chromium `graph.zoom-redraw` reports Scatter viewBox width/height changes and Line legend geometry changes after wrapper zoom while the contract observes no draw and expects the SVG viewBox to remain unchanged. Audit the shared zoom/resizer envelope and component geometry publication; wrapper scaling must not mutate canonical SVG dimensions or legend coordinates during a zoom-only operation, and the no-redraw contract must remain intact.

[open][P2][2026-09-11] ROC enters a redraw loop in the small-viewport layout contract after its owner is already ready. Three serial Chromium repeats of `component.small-viewport.layout-stability` observed 8, 9, and 10 additional `drawRoc` starts during a one-second quiet window, against a maximum of one. Audit `js/components/roc.js:5919+` and `js/shared/componentLayout.js:1624-1648` for observer feedback between responsive measurement, layout publication, and draw completion; coalesce owner-scoped resize work and preserve one settled published frame without weakening the final visibility or diagnostics assertions.

[open][P2][2026-09-11] Surface loses the open state of its reporting disclosure when the small-viewport layout reacts. Three serial Chromium repeats of `component.small-viewport.layout-stability` clicked the visible `.stats-report-panel` open, then observed `open=false` after the layout's two-frame reaction. Audit `js/components/surface.js:3406-3445` and the shared stats-report rendering path at `js/shared/stats.js:2115+`; preserve the owner-scoped report model and reapply the user-visible disclosure state when layout reprojects or replaces the panel.

[open][P2][2026-09-11] Scatter worker completion can miss the inactive owner's statistics projection. The serial `stats.async-owner-completion.contract` case waits 45 seconds after an owner-scoped worker result, but the originating inactive Scatter session and its restored owner DOM still lack the expected statistics. Audit worker result routing, generation checks, session result write-through, and deferred projection on reactivation; stale results must remain ignored while valid inactive-owner results must be stored and projected when that owner becomes active.

[open][P2][2026-09-11] Heatmap heavy recovery and small-tab cache restore are not fully owner/fidelity stable. Serial Chromium reports a recovered title reverting to `Heatmap`, a heavy-tab canvas revision increasing from 1 to 2, and a 5.25 px matrix-left drift after small-tab switching; the owner snapshot otherwise remains consistent. Audit title/config projection, cache validator/rebind, and Heatmap matrix-layer geometry separately from the existing signature and values-cache issues; preserve exact title, cache reuse, and owner geometry across recovery and same-type switching.

[open][P2][2026-09-11] Heatmap immediate data-values recovery can time out at owner payload projection. The serial `recovery.graph-view-controls` case cannot observe the expected values-view owner state within its contract window. Audit recovery ordering between DataViews deserialization, active-view selection, control projection, and owner readiness; publish the owner state only after the selected view is applied and keep the recovery assertion unchanged.

[open][P2][2026-09-11] 3D lock-ratio restoration performs duplicate draws. Serial Chromium `lock-ratio-axis-geometry` observes two `drawScatter` calls for a single forced 3D lock-ratio operation where at most one is expected. Audit shared Plot3D/resizer aspect-lock event propagation and coalescing; preserve one canonical geometry publication and interaction behavior across Scatter, PCA, Line, and Surface.

[open][P2][2026-09-11] Surface live resize shifts the mesh independently of the persisted size. Serial Chromium `surface.live-resize-stability` moves the mesh X coordinate by about 5.25 px after a live resize while the expected canonical geometry is unchanged. Audit Surface's Plot3D viewport/envelope reservation and resize publication against the shared 3D contract; retain mesh, legend, and reopen geometry checks.

[open][P2][2026-09-11] Venn UpSet live-resize performance exceeds the serial responsiveness budget. The serial `venn.upset.live-resize.text-stability` case reports RAF p95 about 50 ms against a 25 ms limit. Profile text measurement, resize scheduling, and redraw coalescing under the supported Chromium worker configuration before changing the budget; preserve text stability and owner isolation.

[open][P2][2026-09-10] Line can continue an asynchronous draw after its active render references have been invalidated. The production-ordered `__tests__/line.view.test.js` suite reproduces this serially: 13/14 tests pass, while `line overlay checkbox intent survives transient unavailable stats restore` logs `drawLine error TypeError: Cannot read properties of null (reading 'style')` at `js/components/line.js:15875`, after repeated owner/session rebinds and DOM projection resets. The initial `refs.plot` check at `js/components/line.js:15405` does not protect the later continuation because the draw uses mutable active mirrors across awaits. Refactor Line drawing around an immutable owner/session/render execution context, with cancellation and commit checks before every projection phase; bind the target plot from that context and invalidate superseded generations before replacing active refs. Do not close this with a null guard or by weakening the test. Add a browser regression for owner switch/reopen during a pending draw and retain the repeated-bootstrap Jest case as a lifecycle characterization gate.

[open][P2][2026-09-10] Box large-draw cancellation does not consistently meet the one-second stop contract. The isolated serial Chromium `box.loading-overlay.stop` case reaches the cancelled overlay state in `1.692 s` after the cancel click, exceeding the `<1,000 ms` acceptance limit; the prior targeted wave measured `1.261 s` in a multi-spec run. The test waits on the owner overlay's cancelled state, so this is not a missing-selector or swallowed-error result. Trace cancellation from the Box owner job through worker termination and overlay publication, identify the delayed phase, and make cancellation acknowledgement owner-scoped and promptly observable without dropping retry/recovery semantics. Add a stable cancellation-generation contract and retain the responsiveness probe; do not merely relax the one-second assertion.

[open][P2][2026-09-10] Box same-component activation remains slower than its no-recompute performance contract. The isolated serial Chromium `box.stats-performance.regression` cases measured `2.506 s` and `2.662 s` for owner activation after replacing the test's fixed 300 ms sleep with owner-readiness polling; both exceed the `<2,000 ms` limit while statistics recomputation and panel capture remain zero. Profile the activation path across `js/main/tabs.js`, `js/components/box.js`, shared layout/cache projection, and readiness publication; preserve session-owned stats and render-cache reuse, then reduce unnecessary work or publish completion earlier only when the owner projection is actually usable. Keep the no-recompute/draw-duration assertions and add phase timings before changing the budget.

[open][P2][2026-09-11] Scatter's heavy-draw loading overlay can lose pointer hit-testing before cancellation is acknowledged. In the serial Chromium `scatter.loading-overlay.threshold` repeat, two runs cancelled successfully but one run had the visible Stop locator intercepted by the Scatter SVG inside `.resizer-zoom-viewport`; the draw then completed and the overlay disappeared. This leaves a real user-facing race in the shared overlay/zoom stacking boundary, not merely an absent selector. Audit `css/style.css:5655-5659,5753-5755` and the Scatter overlay host/controller at `js/components/scatter.js:11799-11808`, then ensure the owner overlay remains the top hit-tested layer for its visible lifetime and that cancellation is acknowledged before a completed draw hides it. Preserve zoom/resizer interactions and owner-scoped job tokens; retain a real locator-click regression and add a separate owner-API cancellation characterization if needed, rather than forcing the click or weakening the stop contract.

[open][P2][2026-09-08] Venn welcome-example persistence still leaves the new tab `payloadDirty` after the six-second recovery window, reproducibly in the isolated `welcome.example-persistence` test, while the other ten components finish clean. The owner-scoped asynchronous species-detection completion can commit derived species state after the shared Welcome payload flush. Decide whether the Venn readiness contract should await that completion or whether the derived lifecycle commit should trigger its own canonical flush; preserve species-detection owner/token safety.

[open][P2][2026-08-21] `Shared.componentLifecycle` now has the first plain-data semantic render-impact contract (`paint`, `layout`, `analysis`, and `structural` with strongest-impact merging), and ROC is migrated as the first consumer. The remaining shared/component controls still call schedulers without declaring their maximum impact, so broader migration is needed to prevent local paint/layer changes from coalescing into broader redraws. Extend the contract incrementally to state-only/named layers/scales where needed, preserve synchronous canonical write-through and immediate owner cache invalidation, and add redraw-impact coverage for each migrated control. This is an architecture/performance issue, not evidence of current cross-tab corruption.
[open][P2][2026-08-21] Welcome advertises `.txt` import (`index.html:170`) and `Shared.tableImport` lists TXT as a supported welcome/table format, but `openWelcomeFile()` in `js/main.js:1092-1119` dispatches only CSV/TSV/XLSX/XLS/ODS through the tabular import branch. TXT therefore falls through as unsupported despite the UI contract. Route TXT through the same delimited-text import path and add picker/drop/welcome regression coverage.


[open][P2][2026-09-05; rechecked 2026-09-10] Axis tick-label/title clearance is not resolved from one optical measurement. Standard 2D renderers place titles from `Shared.chartStyle.computeBottomLayout()` using `axisTitleGap` (`0.75em`); Chromium measurements show that this metric is the main visible clearance, while the full title-font term is baseline/reserve arithmetic rather than an additional visible gap. The serial `cartesian.proactive-x-label-reserve` case still reproduces a Line title bottom of `460.765` against the frame-plus-one limit of `459`, confirming this remains an open renderer contract issue rather than only a parallel-load failure. `Shared.computeAxisLabelYOffset()` and `dy=0.35em` are baseline compatibility and should not be removed without separate export validation. Venn/UpSet, Surface/Plot3D, and PCA scree use separate formulas, so spacing can diverge across axis-containing components. First reduce and test the shared `axisTitleGap` (candidate: about `0.35em`, 4 px minimum), then normalize the renderer-specific paths without changing rotated-label projection or Cartesian envelope rules.

[open][P2][2026-09-05] Line's pre-existing area-fill and regression confidence/prediction interval layers are still emitted inside the per-series render loop. A later series fill/interval can therefore paint over an earlier series line or markers. The new grouped-replicate uncertainty-band implementation deliberately avoids this by using one global background band layer; normalize the older area/regression fill layers to the same global background-layer contract in a dedicated refactor with multi-series z-order regression coverage.

[open][P2][2026-09-11] PCA restored point-format interaction is not ready after same-component archive/cache rebind. The Chromium standalone `04a_reopened_first_interaction` contract reaches both restored PCA tabs, but the point-format click on the second tab does not expose the owner-scoped symbol toolbar within 30 seconds; the phase log records the graph-edit click and then no toolbar publication. `js/components/pca.js:3273+` binds point-format handlers during rehydration, while cached same-component activation in `js/components/pca.js:18869+`/the shared lifecycle reuses the projection; verify that restored point nodes, their owner metadata, and the symbol-toolbar host are rebound before first interaction, and add a settled two-tab reopen regression without weakening the owner/readiness checks.
[open][P3][2026-08-21] Owner-aware control binding remains duplicated in 10 components: `bindBoxControlHandler`, `bindScatterControlHandler`, `bindPcaControlHandler`, `bindLineControlHandler`, `bindHeatmapControlHandler`, `bindSurfaceControlHandler`, `bindRocControlHandler`, `bindSurvivalControlHandler`, `bindHistControlHandler`, and `bindPieControlHandler`. Consolidate the common owner-resolution/deduplication/lifecycle behavior into one shared binder, retain component-specific hooks only where semantically required, migrate each component with regression coverage, and delete local implementations only after confirming they are unused. Venn's differing binding model should be evaluated explicitly rather than force-fit.

[open][P3][2026-08-25] Isolated Jest component suites still duplicate production module bootstrap lists, so new shared dependencies can be omitted without an immediate structural failure. `__tests__/heatmap.stats.test.js` drifted this way when `statsInference.js` became a Heatmap dependency, producing misleading statistics failures until the harness was normalized. Introduce one dependency-aware component-test bootstrap helper (or generated dependency contract) that mirrors the production bundle order, migrate isolated suites incrementally, and retain explicit per-suite additions only for deliberate test doubles.

[open][P3][2026-08-21] Ownership-sensitive modules remain too large for reliable reasoning and safe maintenance. Current line counts include Box 39,768; Scatter 30,945; Line 19,510; PCA 19,234; `shared/hot.js` 18,709; Heatmap 14,993; and Venn 13,207 lines. Refactor behavior-preservingly along ownership/state/payload/table/control/render/statistics boundaries, with explicit public contracts and dependency direction; use existing owner/reopen tests as characterization gates and avoid simultaneous behavioral fixes during mechanical extraction unless separately reviewed.

[open][P3][2026-08-21] The Box critical layout suites no longer use arbitrary transition sleeps: `box.significance-layout.spec.js` retains only one explicit 1,400 ms no-further-redraw measurement, `box.stats-performance.regression.spec.js` retains 40 ms live-drag pacing, and `box.dual-tab.significance-resize.isolation.spec.js` retains its deliberate 1,600 ms quiet-period performance probe. Owner-scoped readiness now governs the surrounding transitions. Keep these timing lanes explicit and separate from ordinary deferred waits; do not replace them without preserving the stability/performance contract.

[open][P3][2026-08-21] Shared axis-control discovery still has a geometry fallback tied to SVG `<line>` primitives (`js/shared/axisControls.js:3527-3563`): when explicit `data-axis-line` metadata is absent it infers x/y axes from x1/y1/x2/y2 orientation. This is brittle for compound/path/multi-segment broken axes. Extend semantic axis-segment metadata and make controls consume that representation before further compound-path migration; retain geometric inference only as a verified legacy fallback until all component renderers emit semantic ownership/axis metadata.

[open][P3][2026-08-21; rechecked 2026-09-02] Color-picker numeric/thickness popup construction is duplicated across shared toolbars and is not fully normalized around the canonical `Shared.workspaceToolbar.attachColorPickerNumericSection()` (`js/shared/workspaceToolbar.js:1114`). Font controls already use the shared helper (`js/shared/fontControls.js:5171`), but equivalent local builders remain in `js/shared/additionalLineControls.js` (`attachStylePickerThicknessSection`), `js/shared/axisControls.js` (`attachAxisStylePickerThicknessSection`), `js/shared/gridControls.js` (`attachStylePickerThicknessSection`), and `js/shared/significanceControls.js` (`attachSignificanceStylePickerThicknessSection`). `js/shared/symbolToolbar.js` also independently constructs its Size and Border thickness sections through `createStyleSection`, with custom interaction and undo handling. These copies currently repeat DOM construction, numeric input setup, popup cleanup, value mirroring, and parts of wheel/change behavior. As a result, a future fix or contract change in the shared builder can silently omit sibling toolbars, causing divergent formatting, validation, min/max/step handling, units, input/change commit semantics, cancel behavior, undo grouping, accessibility, popup placement, cleanup, or owner/session binding. The recently added two-decimal px display behavior reduces the visible inconsistency but does not remove this architectural duplication; Line and Venn direct value labels are separate display paths and are not popup-builder instances. Refactor by extending the shared builder, or extracting its genuinely common numeric-section primitive, with explicit options for labels, units, ranges, source/mirror inputs, insertion position, interaction callbacks, and undo policy. Migrate only semantically equivalent sections; keep Symbol's feature-specific state/undo adapter explicit if it cannot use the full helper without behavior changes. Before deleting any local builder, verify all call sites with repository search. Acceptance: one shared construction path for equivalent numeric color-picker sections; identical wheel, validation, formatting, mirroring, commit/cancel, cleanup, accessibility, and owner-binding contracts across migrated toolbars; no change to canonical/session/payload precision; targeted Jest coverage for each migrated variant plus representative Chromium checks for popup lifecycle, wheel commit/cancel, undo, and same-component tab isolation. This is maintainability and behavioral-drift debt, not the confirmed root cause of the original Line screenshot.

[open][P3][2026-08-21] Multiple components retain local p-value formatting fallbacks after first attempting the shared reporting/formatter API (Line, Scatter, Box, Histogram, Pie, ROC, Survival). These fallbacks are useful for isolated loading/tests but can drift in decimal/scientific threshold notation and equality semantics. Move the minimal canonical formatting primitive into a dependency-safe shared bootstrap, make component fallbacks delegate to that primitive, and add formatter equivalence tests before removing duplicate branches.

[open][P3][2026-08-21] Shared p-value formatting silently clamps finite inputs outside the valid probability range `[0, 1]` before presentation. A negative upstream result is therefore displayed as an extreme valid probability instead of exposing the statistical defect, while values above one become `1`. Define one invalid-probability reporting contract, return an explicit unavailable/error state for out-of-range inputs, and add shared/component tests without weakening the separate floating-point underflow threshold behavior for a legitimate zero result.

[open][P3][2026-08-21] Specialized logistic-regression inference still contains a separate two-parameter Fisher-information solve/covariance implementation alongside the shared pivoted-QR/nonlinear-inference machinery in `js/shared/regression.js`. No current numerical failure was established, so this is consistency/conditioning debt rather than a correctness bug. Add oracle cases spanning separation, near-singular information, extreme scales, and ordinary fits; compare diagnostics/intervals against the shared backend; migrate only where the mathematical contract is equivalent and preserve model-specific inference where required.

[open][P3][2026-08-23; rechecked 2026-09-05] Component session shape guards are not uniformly idempotent. Histogram, ROC, PCA, and Venn still rebuild or clone already-valid durable `session.state` (and, where applicable, results/notes) on routine owner lookup; Pie was normalized while fixing horizontal-resize churn. Introduce one shared idempotent session-shaping contract, migrate sibling components with tab-isolation/reopen tests, and preserve object identity until an explicit hydration or state replacement occurs.

[open][P3][2026-09-05] The new manual x-axis tick-label angle feature is normalized only across the shared 2D axis-controls/cartesian reserve pipeline (Box, Scatter, Line, Histogram, PCA, Pie stacked bar, ROC, Survival, and UpSet in Venn). `js/components/surface.js` still uses a distinct 3D projection/rendering path with no shared x-axis control or bottom-reserve contract, so any future manual tick-angle control for Surface should be handled as a separate 3D-axis refactor rather than by force-fitting the 2D reserve/orientation helpers.
[open][P3][2026-09-05] Grouped-replicate Line uncertainty is currently fixed to mean ± sample SD. The toolbar now labels that measure explicitly and both error bars and shaded bands consume the same computed bounds. If SEM or confidence-interval measures are added later, implement one owner-scoped uncertainty-measure setting and one shared computation path so changing presentation can never change the statistical definition implicitly.

[open][P3][2026-09-11] PCA has no owner-scoped `hasRenderedGraph()` probe, so the shared test readiness contract cannot certify primary-frame publication for PCA after a cached DOM rebind. `js/components/pca.js` uses `Shared.componentLifecycle.hasRenderableGraphContent()` internally during draw, but does not expose the equivalent owner-aware component method implemented by Box, Scatter, Heatmap, Venn, and ROC. Add a PCA publication probe that resolves the requested tab/root and distinguishes a committed owner frame from merely mounted SVG; then add a readiness and cached-rebind regression without weakening the existing visible-graph/data assertions.
[open][P3][2026-09-11] Statistical figure-summary invalidation lacks a cross-component stale-report regression. Box previously cleared its visible report host without clearing `Shared.statsFigureSummary`'s owner-scoped registry, so changing analysis settings could leave the graph summary describing the prior test. Add a focused serial browser contract covering every figure-summary publisher: Box, Scatter, Line, Histogram, PCA, Pie, ROC, Survival, Heatmap, Surface, and Venn. After invalidation and replacement, assert that the old summary is absent or replaced, rendered text and the per-tab registry match the new report, and no pending render can restore the old model. Keep this separate from the ongoing test-infrastructure migration.
