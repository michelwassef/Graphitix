# Open Issues (Reviewed 2026-09-18; session-shaping, owner-binding, logistic-inference, semantic-axis, shared color-picker, coverage-certification, semantic-render-impact, Heatmap runtime, Box statistics, point-sizing, figure-summary layout, and test-suite governance issues fixed 2026-09-14/18)
# Actionable backlog only. Resolved, stale, duplicate, speculative, and metric-only entries are intentionally excluded.
# Ordered by recommended fix sequence. P0-P3 labels describe severity/evidence class; they do not override this execution order.
# Priority: P0 = confirmed durable cross-tab/data-corruption risk; P1 = high-impact correctness/isolation/recovery root cause; P2 = medium correctness, certification, performance, or architecture risk; P3 = maintainability/test/tooling debt.
# The 2026-08-21 audit was source/static; later runtime evidence supplied by the user is explicitly identified in dated follow-up entries. Deterministic source/generated-asset checks are identified explicitly below.
# 2026-08-22 recheck: the former Raw-DataView, per-tab significance-threshold, delayed graph-sizing retargeting, and dynamic palette owner-write-through findings are source-resolved in the current tree and therefore removed from the actionable backlog. The palette recheck normalized Box, Line, Scatter, and Survival to exact-owner write-through instead of accepting component-local mirror mutation as persistence. The new exhaustive parameter-isolation suites still require local runtime certification.

# Recommended execution order, based on current evidence:
# 1. Large-module extraction: valuable maintainability work, but the broadest and riskiest refactor; do it after the ownership and statistical contracts are settled.
#
# If a reproducible cross-tab leak or saved-data loss is found while working on any item, that confirmed P0/P1 defect supersedes this latent-risk order.

[open][P3][2026-08-21; rechecked 2026-09-18] Ownership-sensitive modules remain too large for reliable reasoning and safe maintenance. The reviewed behavior-preserving extraction wave now covers Box point-sizing, Hot filter state, Heatmap palette/scale state, PCA table-schema/label-row normalization, Line overlay modeling, Scatter label distribution, and Venn circle/rectangle geometry, with focused Box, table, Heatmap, PCA, Line, Scatter, Venn, archive, undo, and Chromium characterization coverage passing. Deeper ownership/state/payload/table/control/render/statistics boundaries remain in the large modules; continue only in reviewed slices and do not combine extraction with behavioral fixes.

[resolved][P3][2026-09-18] Test organization and governance are now enforced for the active Chromium scope: 0 Jest suites and 1 intentionally deferred Firefox-only Playwright suite remain at legacy roots; all 20 oversized suites are grouped, the manifest reports 566 rows with 0 unmapped and 774 explicit/0 inferred mappings, and the all-eleven lifecycle, old-cache, rollback, and lazy-hydration contracts pass. Broader production-fixture adoption, capability-specific fallback matrices, and fresh release certification remain separate non-blocking roadmap work.
