[2026-05-01] Resize-flow duplication: box.js and scatter.js each implement bespoke resizer phase gating logic. This should be centralized in a shared helper (e.g., Shared.componentLayout/Shared.resizer) to keep behavior homogeneous across components and reduce regression risk.
[2026-05-01] Legacy backup source: js/shared/componentLayout.js.bak remains in the source tree alongside js/shared/componentLayout.js. It should be removed or moved out of the runtime source area after confirming it is not referenced, to avoid stale-code confusion during searches and maintenance.
[2026-05-02] Axis-resize margin stabilizer duplication: scatter.js and line.js keep component-local copies of margin-lock logic instead of relying on chartStyle.stabilizeAxisResizeMargins. The duplicated scatter copy diverged from the shared behavior during horizontal resize, so these should be consolidated behind the shared helper.
[2026-05-02] AG Grid option drift: heavy archive load logs show AG Grid rejecting gridOptions.columnBuffer and zero-height getRowHeight results. The shared table setup should be aligned with the current AG Grid API so heavy datasets do not accumulate avoidable grid warnings during restore/open.
[2026-05-02] Scatter activation initialization order: scatter setup bound graph-type activation handlers before scatter label state variables were initialized, allowing same-component activation to hit a temporal-dead-zone ReferenceError. Fixed in the cache work by moving label state initialization before synchronous handler binding; future scatter setup state should stay above controls that may dispatch during activation.
