--- a/js/components/line.js
+++ b/js/components/line.js
@@ -10446,40 +10446,6 @@
     });
   }
 
-  function commitLine3dRotationFrame(liveSvg, stagedSvg){
-    if(!liveSvg || !stagedSvg || liveSvg === stagedSvg){
-      return false;
-    }
-    const preservedIdentity = liveSvg.dataset?.e2eRotationIdentity || '';
-    const preservedId = liveSvg.id || 'lineSvg';
-    const stagedAttributes = Array.from(stagedSvg.attributes || []);
-    Array.from(liveSvg.attributes || []).forEach(attribute => {
-      const name = attribute?.name || '';
-      if(name === 'id' || name === 'data-e2e-rotation-identity' || name === 'data-rotation-controls-attached'){
-        return;
-      }
-      liveSvg.removeAttribute(name);
-    });
-    stagedAttributes.forEach(attribute => {
-      const name = attribute?.name || '';
-      if(!name || name === 'id' || name === 'data-e2e-rotation-identity' || name === 'data-rotation-controls-attached'){
-        return;
-      }
-      liveSvg.setAttribute(name, attribute.value);
-    });
-    liveSvg.id = preservedId;
-    if(preservedIdentity){
-      liveSvg.dataset.e2eRotationIdentity = preservedIdentity;
-    }
-    liveSvg.replaceChildren(...Array.from(stagedSvg.childNodes));
-    liveSvg.dataset.viewMode = '3d';
-    liveSvg.style.pointerEvents = 'all';
-    if(typeof plot3d.ensureRotationHitSurface === 'function'){
-      plot3d.ensureRotationHitSurface(liveSvg, { debugLabel: 'line-3d-rotation-frame' });
-    }
-    return true;
-  }
-
   function bindLine3dRotationControls(svg, debugLabel){
     if(!svg || !svg.dataset || svg.dataset.viewMode !== '3d'){
       return false;
@@ -12317,7 +12283,11 @@
       budgetMs: 10,
       drawOptions: drawOpts
     }) || null;
+    const liveRotationDraw = drawOpts?.reason === 'rotation';
     const checkpoint = async () => {
+      if(liveRotationDraw){
+        return execution?.isCurrent?.() !== false;
+      }
       try{
         await execution?.checkpoint?.();
       }catch(err){
@@ -12329,8 +12299,6 @@
       return execution?.isCurrent?.() !== false;
     };
     let svgPublication = null;
-    let rotationFrameTarget = null;
-    let rotationFrameStaged = null;
     try{
       const debugStamp = Date.now();
       console.debug('Debug: drawLine3d start', { debugStamp });
@@ -12684,26 +12652,27 @@
       plotEl.style.backgroundColor = '';
       plotEl.style.boxSizing = 'border-box';
       const existingLineSvg = plotEl.querySelector?.('#lineSvg') || null;
-      const isRotationFrame = drawOpts?.reason === 'rotation'
+      const reuse3dSvg = liveRotationDraw
         && existingLineSvg
         && existingLineSvg.dataset?.viewMode === '3d';
-      const svg3 = global.document.createElementNS(NS, 'svg');
+      const svg3 = reuse3dSvg
+        ? existingLineSvg
+        : global.document.createElementNS(NS, 'svg');
       svg3.setAttribute('width', String(W3));
       svg3.setAttribute('height', String(H3));
       svg3.setAttribute('viewBox', `0 0 ${W3} ${H3}`);
       svg3.setAttribute('font-family', chartStyle.FONT_FAMILY);
       svg3.dataset.viewMode = '3d';
       chartStyle.prepareSvg(svg3, { scopeId: 'line' });
-      if(isRotationFrame){
-        rotationFrameTarget = existingLineSvg;
-        rotationFrameStaged = svg3;
+      if(reuse3dSvg){
+        svg3.replaceChildren();
       }
       svg3.style.backgroundColor = lineThemeDark
         ? normalizeLineThemeColor(lineThemeState.backgroundColor, '#000000')
         : '';
       svg3.style.pointerEvents = 'all';
       svg3.setAttribute('data-color-scheme', lineThemeState.colorScheme || 'scientific');
-      if(!isRotationFrame){
+      if(!reuse3dSvg){
         svgPublication = Shared.framePublication.stage({
           container: plotEl,
           frame: svg3,
@@ -12715,10 +12684,10 @@
         });
       }
       appendLine3dBackground(svg3, W3, H3, invocation.session);
-      if(!isRotationFrame){
+      if(!reuse3dSvg){
         svg3.addEventListener('mouseleave', handleLinePlotMouseLeave);
-        bindLine3dRotationControls(svg3, 'line-3d');
       }
+      bindLine3dRotationControls(svg3, 'line-3d');
 
       const legendAxisGap = Math.max(fs * 0.9, 18);
       const appliedLegendAxisGap = showLegend ? legendAxisGap : 0;
@@ -13283,12 +13252,7 @@
       if(!(await checkpoint()) || (invocation.session && !isLineSessionActive(invocation.session))){
         return false;
       }
-      if(rotationFrameTarget && rotationFrameStaged){
-        if(execution?.isCurrent?.() === false || (invocation.session && !isLineSessionActive(invocation.session))){
-          return false;
-        }
-        commitLine3dRotationFrame(rotationFrameTarget, rotationFrameStaged);
-      }else if(svgPublication){
+      if(svgPublication){
         svgPublication.commit();
       }
       getActiveLineLayoutManager()?.syncPanels?.({ skipSchedule: true });
--- a/e2e/line-scatter.3d-rotation-tab-switch.spec.js
+++ b/e2e/line-scatter.3d-rotation-tab-switch.spec.js
@@ -137,7 +137,9 @@
       running: true,
       samples: 0,
       minChildCount: Number.POSITIVE_INFINITY,
-      minMarkCount: Number.POSITIVE_INFINITY
+      minMarkCount: Number.POSITIVE_INFINITY,
+      minViewBoxWidth: Number.POSITIVE_INFINITY,
+      minViewBoxHeight: Number.POSITIVE_INFINITY
     };
     const sample = () => {
       const probe = window.__rotationFrameProbe;
@@ -151,6 +153,9 @@
         probe.minMarkCount,
         activeSvg?.querySelectorAll('path,polyline,line,circle,polygon').length || 0
       );
+      const viewBox = activeSvg?.viewBox?.baseVal;
+      probe.minViewBoxWidth = Math.min(probe.minViewBoxWidth, Number(viewBox?.width) || 0);
+      probe.minViewBoxHeight = Math.min(probe.minViewBoxHeight, Number(viewBox?.height) || 0);
       requestAnimationFrame(sample);
     };
     requestAnimationFrame(sample);
@@ -176,13 +181,17 @@
       retainedIdentity: svg?.dataset?.e2eRotationIdentity === identityToken,
       samples: probe?.samples || 0,
       minChildCount: Number.isFinite(probe?.minChildCount) ? probe.minChildCount : 0,
-      minMarkCount: Number.isFinite(probe?.minMarkCount) ? probe.minMarkCount : 0
+      minMarkCount: Number.isFinite(probe?.minMarkCount) ? probe.minMarkCount : 0,
+      minViewBoxWidth: Number.isFinite(probe?.minViewBoxWidth) ? probe.minViewBoxWidth : 0,
+      minViewBoxHeight: Number.isFinite(probe?.minViewBoxHeight) ? probe.minViewBoxHeight : 0
     };
   }, { svgSelector: selector, identityToken: token });
   expect(rotationFrameResult.retainedIdentity, `${selector} should retain the active SVG throughout a drag`).toBe(true);
   expect(rotationFrameResult.samples, `${selector} should be sampled across painted rotation frames`).toBeGreaterThan(2);
   expect(rotationFrameResult.minChildCount, `${selector} should never publish a blank rotation frame`).toBeGreaterThan(0);
   expect(rotationFrameResult.minMarkCount, `${selector} should keep graph marks visible throughout rotation`).toBeGreaterThan(0);
+  expect(rotationFrameResult.minViewBoxWidth, `${selector} should retain a measurable viewport during rotation`).toBeGreaterThan(100);
+  expect(rotationFrameResult.minViewBoxHeight, `${selector} should retain a measurable viewport during rotation`).toBeGreaterThan(100);
 }
 
 async function expectRotationAfterTabSwitch(page, component, tabId, awayComponent, label) {
--- a/CHANGELOG.md
+++ b/CHANGELOG.md
@@ -1,3 +1,10 @@
+## 2026-07-30 — Smooth Line 3D live rotation
+
+- Kept Line's mounted 3D SVG as the live rotation surface so pointer capture remains uninterrupted.
+- Suppressed cooperative rendering yields only for the bounded, view-only rotation frame, preventing the reused SVG from being painted between clear and rebuild.
+- Removed detached-SVG rotation staging because SVG geometry measurement on an unmounted frame collapsed its viewport to the padding-only fallback.
+- Extended the Line/Scatter browser regression to require non-empty marks and a valid measurable viewport in every sampled animation frame.
+
 ## 2026-07-30 — Smooth Line 3D rotation publication
 
 - Line 3D rotation now renders each replacement frame off-DOM and atomically transfers it into the still-mounted owner SVG, preserving pointer capture without exposing a cleared intermediate frame.
